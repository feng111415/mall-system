# 商城系统验收记录

## V0.3 模块 1：订单详情与支付时限

验收日期：2026-07-29
分支：`dev`
基线提交：`05e0b2b docs: 记录商城 V0.2 验收复核结果`

### 自动化与构建

- 后端：`mvn -pl mall-business -am test`，65 项通过，0 失败。
- 用户端：`npm.cmd run build`，生产构建通过。
- 后端：`mvn -pl ruoyi-admin -am package -DskipTests`，完整打包通过。
- API：`scripts/mall-storefront-e2e.ps1`，17 项通过。
- MyBatis：14 个 Mapper XML 均可解析。

### 业务验收

- 登录、商品浏览、重复加购、地址新增/修改/默认切换/删除、结算、幂等下单、创建支付、模拟成功、订单查询全链路通过。
- 创建支付后订单为 `PAYING`；详情包含 30/35 分钟截止时间、商品快照、操作记录和支付记录。
- 另一会员查询该订单支付记录返回空集合，未泄露幂等键。
- 将隔离库测试订单推进到第 35 分钟后模拟迟到支付，结果为订单 `CLOSED`、支付 `REFUNDED`、库存预占 `RELEASED`，且存在唯一 `LATE_PAYMENT_REFUND` 补偿任务。

### 数据库与浏览器

- 临时库从空库全量升级至 V2.8，通过；V2.8 回滚后重新升级，通过。
- 回滚验证覆盖 `REFUNDED` 数据归一化及新增列、唯一索引、检查约束的撤销。
- Microsoft Edge 订单详情在 1440px 和 390px 视口通过；`scrollWidth` 等于视口宽度，无控制台错误、失败请求或图片加载失败。

### 超时任务回归补充

- 根因：`MallOrderTimeoutTask` 有实现但未注册到 `sys_job`，Quartz 不会自动执行关闭逻辑。
- 修复：新增 V2.8.1 幂等任务迁移，`mallOrderTask.closeExpiredOrders` 每分钟执行一次；回滚后任务数为 0，重新升级后任务数为 1。
- 实测：同一会员的 3 笔过期待付款订单均在调度周期内关闭，其中 2 笔为 `CLOSED + UNPAID`、1 笔为 `CLOSED + FAILED`；3 条预占库存均为 `RELEASED`，进行中待付款订单数为 0。
- 上限回归：该会员随后通过真实 API 成功创建新订单，证明 `CLOSED` 订单不会计入 3 笔未支付限制。

### 审查结论

- 支付时间窗集中在 `MallOrderPaymentWindow`，订单模块通过 `PaymentExpirationPort` 调用支付过期能力。
- 支付行和订单行锁顺序保持为支付后订单，超时任务与支付回调竞态可收敛。
- 迟到支付状态与补偿任务已纳入同一准备事务，即时退款失败时由现有补偿任务重试。
- 模块 1 满足提交条件；后续模块不得在本提交中混入。

## V0.2 验收与提交复核

执行日期：2026-07-29

执行分支：`dev`

验收前提交：`5886235 docs: 汇总商城 V0.2 完成交接信息`

### 构建与单元测试

- 商城用户端生产构建：`npm.cmd run build`，通过，Vite 共转换 372 个模块。
- 商城后端全量测试：`mvn -pl mall-business -am test`，53 项通过，0 失败、0 错误、0 跳过。
- 后端可执行 JAR：`mvn -pl ruoyi-admin -am package -DskipTests`，通过。
- 测试存在 Mockito 动态加载 Java Agent 的未来兼容性警告，不影响本次结果，后续升级 JDK 或 Mockito 时需处理。

### 真实全链路

- API 验收脚本：`scripts/mall-storefront-e2e.ps1`，14 项通过。
- API 链路覆盖：健康检查、匿名访问拦截、商品与 SKU、短信登录、重复加购、地址新增/修改/删除/默认切换、结算预览、订单幂等、Mock 支付、订单详情与列表。
- 真实 Edge UI 链路：通过。
- UI 链路覆盖：手机号验证码登录、商品浏览、商品详情、加购、地址新增并设默认、购物车、结算、提交订单、创建支付单、模拟支付成功、订单列表待发货状态。
- 浏览器验收未捕获控制台错误或页面异常。

### 数据库迁移

- 验证库：`mall_migration_verify_20260729_1059`，验收结束后已删除。
- 从空库依次执行 `ry_20260417.sql`、`quartz.sql` 以及全部商城版本脚本至 `V2.7.0`，全部通过。
- 迁移后共 57 张表，其中商城表 26 张；8 个上架 SPU、17 个有效 SKU、17 条库存记录、5 条对账权限均符合预期。
- 商城迁移脚本未发现 `DROP TABLE`、`DROP COLUMN`、`RENAME TABLE`、`TRUNCATE TABLE` 或 `DELETE FROM` 等破坏性语句。
- 原业务库 `ry-vue` 未修改；真实链路只写入 `mall_migration_test_20260728`。

### 代码审查与回滚结论

- 审查范围：`v2.6.7..5886235`，共 38 个变更文件。
- 本次构建、单元测试、API 链路和浏览器链路未发现阻断商城功能提交的问题。
- **回滚演练未通过**：仓库没有 `release/v2.6.7` 发布产物，执行 `scripts/rollback.ps1 -Version v2.6.7` 返回发布包不存在。
- 当前 `rollback.ps1` 仅验证本地发布目录是否存在，不执行应用产物替换、健康检查或失败恢复，因此不能视为可执行回滚方案。
- 数据库迁移采用追加式脚本，未提供反向迁移；按现有架构约定，数据库异常应通过修复迁移处理，不应直接回退已有交易数据。

### 验收结论

- 前端构建、后端测试、真实 API 链路、真实 Edge UI 链路和数据库正向迁移：**通过**。
- 应用发布产物与自动回滚能力：**未通过，作为后续发布治理阻断项**。
- 本轮不执行生产发布、不创建发布包、不打包 Docker。

## V0.3 模块 5：会员会话与设备安全

验收日期：2026-07-31
分支：`dev`
基线提交：`aabf714 fix: 显示订单项售后进度`

### 自动化与构建

- 后端：`mvn -pl ruoyi-admin -am package`，商城测试 123 项通过，0 失败；首次重打包因运行中 JAR 锁定失败，停止旧 8080 进程后打包通过。
- 用户端：`npm.cmd run build`，生产构建通过，转换 374 个模块。
- 最终 JAR：显式连接 `mall_migration_test_20260728` 在 8080 启动，健康检查返回 `code=200/status=UP`。

### 数据库与安全策略

- V2.12.0 在测试库完成升级、回滚、再次升级和重复升级，三张表数量依次为 3、0、3，表注释编码正常。
- 浏览器设备标识为 32 位随机十六进制值；设备类型只由后端 User-Agent 判定。
- 数据库会话表和 Redis 键只保存 Token SHA-256 摘要，接口响应及日志未输出摘要或原始 Token。
- 主设备更换验证码用途与登录验证码分离，验证码消费、主设备更新和审计处于同一事务；30 天第 4 次由服务端拒绝。

### 真实链路与浏览器

- 同一会员模拟 3 台移动设备和 2 台电脑；最终仅 2 个移动会话、1 个桌面会话、1 个主设备和 1 个当前会话。
- 第三台手机下线普通手机且保留主手机；新电脑下线旧电脑；两枚旧 Token 均返回业务码 401，新电脑 Token 返回 200。
- 普通手机完成独立短信验证并设为新主设备，剩余更换次数为 2；远程下线另一设备后旧 Token 返回 401，数据库审计为 1 条。
- Edge 1440px/390px 均显示 3 个设备，`scrollWidth == clientWidth`，无控制台错误或失败请求。
- 移动端验证表单展开后自动避让固定底部导航，表单底部与导航顶部保持 12px 间距。

### 审查结论

- 会话容量策略集中在独立策略类，Controller 仅负责 DTO、鉴权上下文和响应包装，SQL 保持在 Mapper。
- 设备、会话和审计职责分离，主设备身份不依赖在线会话，满足退出后保留和重新登录优先保护。
- 模块 5 未新增角色权限、未修改若依运营端、未提前实施模块 6 全站 UI 升级。
