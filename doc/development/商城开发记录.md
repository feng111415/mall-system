# 商城开发记录

## 第一阶段：工程基线与前后端骨架

### 已确认

- 项目名称：日常商店商城系统
- 业务类型：单商户 B2C 实物商城
- 后端形态：若依模块化单体
- 用户端：Vue 3 + Vite + Pinia + Vue Router 4 + Vant 4
- 管理端：Vue 3 + Vite + Pinia + Vue Router 4 + Element Plus
- 用户端首页：静态模型模板 A（陈列型）
- 外部服务：首期使用 Mock Adapter

### 本阶段范围

- 建立 Git `dev` 分支和基础忽略规则。
- 创建用户端与管理端独立前端工程。
- 建立前端环境变量、API 基础地址和构建脚本。
- 新增 `mall-business` Maven 模块，接入若依后端启动模块。
- 新增短信、支付、物流三个 Port 与 Mock Adapter。
- 新增匿名健康检查接口：`GET /api/mall/health`。
- 保留现有 `ruoyi-ui` Vue 2 工程，不进行迁移覆盖。
- 不修改若依公共模块；根 Maven 只增加商城业务模块的模块声明和依赖。

### 验收记录

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 用户端构建 | `cmd /c npm run build` | 已通过 |
| 管理端构建 | `cmd /c npm run build` | 已通过，Element Plus 产生大 chunk 警告 |
| 后端商城模块编译 | `mvn -pl ruoyi-admin -am compile -DskipTests` | 已通过 |
| 后端商城模块打包 | `mvn -pl ruoyi-admin -am package -DskipTests` | 已通过 |
| 页面冒烟检查 | Edge + Puppeteer | 用户端/管理端桌面与手机视图无控制台异常、无图片加载失败、无横向溢出 |
| Git 基线 | `git status --short --branch` | 待提交 |

### 遗留问题

- Mock Adapter 目前只用于联调，不代表真实短信、支付或物流成功。
- 用户端和管理端使用 `createWebHistory`，部署 Nginx 时需要配置 SPA fallback 到 `index.html`。
- 用户端 npm 审计提示 2 个 high 风险；管理端提示 1 个 moderate、2 个 high 风险。依赖升级前需要单独评估兼容性，暂不擅自使用 `npm audit fix --force`。
- 真实启动健康接口尚未完成：本机 MySQL 拒绝项目记录的开发凭据 `root/123456`。未修改数据库密码或数据，待本机凭据恢复后执行 `GET /api/mall/health` 验证。

## 第二阶段：用户与商品（2026-07-27 待验收）

> 当前状态：开发完成，代码已提交到 `dev` 分支，等待用户验收。验收通过后再创建版本标签；未验收前不进入库存、购物车、订单或支付阶段。

### 本周已落地

- 会员身份后端半成品：独立 `mall_member`，未复用后台 `sys_user`。
- 会员短信验证码流程：验证码摘要存储、5 分钟有效、60 秒重发间隔、错误次数限制、成功后一次性消费。
- 会员独立 Token：商城会员会话存入 Redis，使用 `mall_member:login:` 前缀，不复用若依后台管理员 Token。
- 会员资料、协议同意、收货地址和后台会员列表接口骨架。
- 商品中心后端半成品：分类、品牌、SPU、SKU、规格、规格值和商品媒体实体、Mapper、Service、公开接口及后台接口。
- 商品保存使用事务；SPU、SKU 采用软删除方向；上架时要求至少存在可用 SKU。
- 新增数据库迁移草稿：`sql/V0.2.0__mall_member_catalog.sql`，包含第二阶段表结构和少量演示数据。
- 用户端已接入验证码登录、分类/搜索、商品列表和 SKU 详情页面；售罄 SKU 禁止加购。
- 管理端已增加会员、分类/品牌路由和页面，商品页正在替换为 SPU/SKU 管理页面。
- 管理端已接入若依账号密码 + 图形验证码登录、后台 Token、登录守卫和退出登录；商城会员 Token 不能进入管理端。
- 管理端商品、分类、品牌和会员接口已补齐 RBAC `@PreAuthorize` 权限及操作日志注解；迁移脚本新增 19 个权限定义。
- 短信发送增加 Redis 分布式 60 秒锁、手机号/IP 日限额和可注入的开发 Mock 验证码；发送失败会释放锁。
- 商品服务增加 SPU/SKU 编码、价格、库存校验；上架只认可用且未软删除的 SKU；商品管理接口完成权限保护。
- 新增 4 个单元测试，覆盖短信重复点击、商品负价格、负库存和 SPU/SKU 保存。
- 用户端和管理端 Vite 开发代理已分别配置为 `/api -> 8080`、`/admin-api -> 8080`，修正 API 前缀重复问题。

### 已验证

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 后端全依赖编译 | `mvn -pl ruoyi-admin -am compile -DskipTests` | 已通过 |
| 用户端构建 | `cmd /c npm run build` | 已通过 |
| 管理端构建 | `cmd /c npm run build` | 已通过；Element Plus 产生大 chunk 警告 |
| 后端全依赖打包 | `mvn -pl ruoyi-admin -am package -DskipTests` | 已通过 |
| 后端单元测试 | `mvn -pl mall-business -am test` | 4 项通过 |
| MyBatis XML 结构 | PowerShell XML 解析 | 3 个 Mapper XML 通过 |
| SQL 临时 MySQL 8.0 验证 | 初始化、重复执行、软删除复用、上架约束 | 已通过；正式库未执行 |
| 页面冒烟检查 | Puppeteer，桌面/手机、用户端/管理端 | 页面文本、图片、脚本和横向溢出检查通过；后端未启动导致的代理 500 已标注为环境原因 |

### 当前工作区

- Git 分支：`dev`。
- 已确认基线提交：`b469f95`、`d954994`。
- 第二阶段提交只包含商城模块、两个前端、迁移脚本、配置和开发记录；工作区原有 `ai-web/` 未跟踪目录未纳入提交。
- 正式 MySQL 服务仍拒绝 `root/123456` 和空密码；未修改数据库密码，未对正式库执行迁移。
- 本轮临时 MySQL 8.0 验证实例已关闭；验证目录因当前环境删除策略限制暂留在 `output/`，不纳入 Git。

### 下周续做顺序

1. 用户验收用户端验证码登录、商品分类/搜索/详情、SKU 售罄禁用加购。
2. 用户验收管理端若依登录、会员列表、分类/品牌和 SPU/SKU 管理。
3. 提供正式 MySQL 凭据后执行 `V0.2.0__mall_member_catalog.sql`，启动后端联调真实接口。
4. 验收通过后创建第二阶段版本标签，例如 `v0.2.0`；随后才开始库存模块。

### 暂停时已知风险

- 正式迁移 SQL 尚未在项目 MySQL 实例上执行，表结构、生成列及初始化数据已在隔离 MySQL 8.0 临时实例验证，仍需正式库复核。
- Mock 短信适配器不能把验证码正文返回前端；联调验证码的获取方式需要使用开发环境配置或服务端受控日志，不能放宽生产安全规则。
- 公开商品接口使用匿名访问；会员接口虽然有独立 Token 校验，但当前控制器整体标记为匿名，必须继续进行对象级权限和接口边界审查。
- 当前已存在第二阶段提交点，但尚未创建标签；恢复工作时不可使用清理未跟踪文件或硬重置命令。

## 第三阶段：交易核心 - 库存中心（2026-07-27）

> 当前状态：库存中心已开发并完成自动化验证，等待用户验收；验收通过后再进入购物车。

### 已完成

- 新增 `mall_stock` 库存快照：可用、锁定、已售和低库存阈值分开维护。
- 新增 `mall_stock_reservation`：按订单号 + SKU 唯一约束库存预占，重复请求只生效一次。
- 新增 `mall_stock_log`：预占、释放、支付确认和人工调整均写入追加式库存流水。
- 预占使用行锁和 `available_quantity >= quantity` 条件更新，库存不足不会产生负数。
- 未支付订单不能确认扣减；取消/超时释放只处理仍为 `LOCKED` 的预占，重复释放安全无副作用。
- 商品详情从库存快照读取实时可用量；后台新增库存列表、低库存筛选、库存调整和流水查看页面。
- 新增权限：`mall:inventory:list`、`mall:inventory:query`、`mall:inventory:adjust`。
- 新增数据库迁移：`sql/V0.3.0__mall_inventory.sql`，首次执行会从现有 SKU 可用库存初始化库存快照。

### 验证记录

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 库存后端测试 | `mvn -pl mall-business -am test` | 10 项通过（库存 6 项） |
| 管理端构建 | `cmd /c npm run build` | 通过 |
| MyBatis XML 结构 | PowerShell XML 解析 | 库存和商品 Mapper 通过 |

### 验收边界

1. 当前阶段只完成库存能力，订单尚未接入 `InventoryPort`；暂不提供面向用户的下单接口。
2. 执行迁移前请确认正式 MySQL 连接信息；未执行迁移时库存接口不能正常查询。
3. 库存验收通过后再开发购物车，购物车不会占用库存，订单创建时才调用预占。

## 第三阶段：交易核心 - 购物车（2026-07-27）

> 当前状态：购物车已开发并完成自动化验证，等待用户验收；结算按钮暂保持下一阶段占位，不进入订单实现。

### 已完成

- 新增 `mall_cart_item`，按会员 + SKU 唯一约束，同一 SKU 重复加购自动累加。
- 购物车数据通过商城会员 Token 隔离，未登录不能读取或修改其他会员购物车。
- 加购和修改数量都会实时读取库存中心，不锁库存，数量超过可用库存时明确拒绝。
- 售罄、下架或删除商品保留在购物车中并自动取消勾选，不计入合计。
- 当前库存低于购物车数量时显示“仅剩 N 件”，不静默修改数量，并禁止结算。
- 新增用户端购物车接口和持久化 Pinia 状态；购物车数量、勾选、删除和金额均从服务端刷新。
- 新增数据库迁移：`sql/V0.4.0__mall_cart.sql`。

### 验证记录

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 购物车及全量后端测试 | `mvn -pl mall-business -am test` | 15 项通过（购物车 5 项） |
| 用户端构建 | `cmd /c npm run build` | 通过 |
| MyBatis XML 结构 | PowerShell XML 解析 | 3 个 Mapper XML 通过 |

### 验收边界

1. 当前只完成购物车，订单、结算、库存预占尚未接入。
2. “去结算”按钮暂时禁用，避免进入尚未实现的订单页面。
3. 购物车验收通过后再开始订单与结算模块。

## 第三阶段：订单与结算 - 第 1 步：订单模型（2026-07-27）

> 当前状态：订单表结构和状态机已完成，等待确认；本步骤不包含结算、下单、库存预占或支付。

### 已完成

- 新增订单主表 `mall_order`，包含订单状态、支付状态、金额、收货地址快照、幂等键和版本号。
- 新增订单明细表 `mall_order_item`，保存商品名称、SKU、规格、图片、单价和数量快照。
- 新增订单操作日志表 `mall_order_operation_log`，记录状态变更前后值、操作人、来源和请求追踪号。
- `member_id + idempotency_key` 唯一约束，保证重复提交不会创建第二笔订单。
- 定义订单状态：待付款、风控审核、待发货、已发货、已完成、售后中、已取消、已关闭。
- 定义独立支付状态，避免从订单状态错误推断支付结果。
- 新增状态机测试，禁止跳过发货、终态重新打开等非法流转。
- 新增迁移文件：`sql/V0.5.0__mall_order_schema.sql`。

### 验证记录

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 订单状态机测试 | `mvn -pl mall-business -am -Dtest=MallOrderStatusTest test` | 3 项通过 |
| 全量后端测试 | `mvn -pl mall-business -am test` | 18 项通过 |
| SQL 静态检查 | PowerShell | 表结构和幂等约束存在 |

### 下一步

确认订单状态设计后，再做第 2 步：结算页读取购物车并展示金额、收货地址和库存校验结果。

## 第三阶段：订单与结算 - 第 2 步：结算预览（2026-07-27）

> 当前状态：结算预览已完成并通过测试，等待验收；本步骤不创建订单、不预占库存、不接入支付。

### 已完成

- 新增 `GET /api/mall/checkout/preview`，通过会员 Token 返回结算预览。
- 订单模块只调用购物车服务和会员地址服务，不直接访问购物车表或库存表。
- 重新读取已勾选商品、实时价格、实时库存和商品有效状态。
- 展示收货地址、默认地址、商品金额、运费、优惠金额和应付金额。
- 库存不足、商品失效、未选择商品或没有收货地址时禁止进入下一步，并返回明确提示。
- 用户端新增 `/checkout` 结算预览页面；提交订单按钮保持禁用，等待下一步实现。

### 验证记录

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| 结算预览测试 | `mvn -pl mall-business -am -Dtest=MallCheckoutPreviewServiceTest test` | 3 项通过 |
| 全量后端测试 | `mvn -pl mall-business -am test` | 21 项通过 |
| 用户端构建 | `cmd /c npm run build` | 通过 |

### 下一步

确认结算预览后，再做第 3 步：创建订单、保存商品快照并生成订单号。

## 商城 V0.2：前五项体验升级（2026-07-28）

> 当前状态：第 1 至第 5 项已开发并完成真实浏览器验证，第五项已通过用户验收；第六项已通过验收；第七项已完成最终回归和版本收尾。

### 已完成

- 接口与数据基线：8 个演示 SPU、17 个 SKU、17 条库存快照及商品查询契约。
- 商品浏览：首页真实商品、分类、搜索、销量与价格排序、加载骨架和空状态。
- 全局框架：桌面导航、移动五栏、统一通知和购物车角标同步。
- 商品详情与加购：SKU、价格、库存、数量边界、售罄控制、相关推荐和最近浏览。
- 购物车体验：全选、数量调整、失效清理、库存与金额反馈、移动端粘性结算栏。
- 结算体验：地址选择、费用明细、订单备注、三步进度、Mock 支付结果和订单入口。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 后端全量测试 | 53 项通过，0 失败 |
| 用户端生产构建 | 通过 |
| API 全链路 | 12 项通过 |
| Edge 真实业务链路 | 加购、全选、改数量、结算、备注、下单和支付通过 |
| 桌面/移动布局 | 无横向溢出或关键元素重叠 |
| 订单备注 | 已通过订单详情接口确认落库 |

### Git 记录

- 提交：`cf2ef81 feat: 完成商城 V0.2 前五项体验升级`
- 分支：`dev`
- 远程：`origin/dev` 已同步
- `ai-web/` 保持未跟踪，未纳入提交

## 商城 V0.2：第六项个人中心与地址簿体验优化（2026-07-29）

> 当前状态：已完成开发、构建、API 回归和 Edge 桌面/移动端真实交互验证，并通过验收。

### 已完成

- 用户端会员 API 增加地址修改和删除调用，继续复用后端地址归属校验。
- 个人中心地址簿使用新增/编辑共用表单，保留省市区级联和邮政编码字段。
- 增加收货人、手机号、地区、详细地址的字段级校验和错误反馈。
- 支持设为默认地址；服务端按会员归属清理原默认地址，列表刷新后保持默认排序。
- 删除地址增加页面内确认、请求忙碌态和成功/失败通知，编辑中的地址删除后会关闭表单。
- 地址卡片增加编辑、设默认、删除操作，并针对移动端改为单列操作区。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 用户端生产构建 | 通过 |
| 后端全量测试 | 53 项通过，0 失败 |
| API 全链路 | 14 项通过，覆盖地址更新、默认切换、删除和默认恢复 |
| Edge 真实业务链路 | 新增、编辑、设默认、删除和删除后空状态通过 |
| 390px 移动布局 | `scrollWidth=390`，无横向溢出 |

## 商城 V0.2：第七项最终视觉验收、回归与收尾（2026-07-29）

> 当前状态：已完成。V0.2 全部阶段交付，当前没有进行中的需求。

### 已完成

- 用户端生产构建通过。
- 后端 `mvn -pl mall-business -am test`：53 项通过，0 失败。
- `scripts/mall-storefront-e2e.ps1`：14 项通过，覆盖登录、商品、购物车、地址 CRUD、结算、幂等下单与 Mock 支付。
- Edge 最终视觉审查覆盖首页、商品列表、商品详情、购物车、结算、订单、个人中心共 7 个核心路由，在 1440px 与 390px 共 14 个视图下均无横向溢出、控制台错误或图片加载失败。
- 总交接、开发进度和桌面副本已统一为 V0.2 完成状态。

### 后续入口

等待用户给出新的版本或功能范围；恢复工作时先读取 `PROJECT_HANDOFF.md` 与 `CONTEXT.md`。

## 商城 V0.3：模块 1 订单详情与支付时限（2026-07-29）

> 当前状态：开发、迁移、回归、真实链路和 Edge 桌面/移动验收已完成；本模块提交后等待用户确认下一模块。

### 已完成

- 新增独立订单详情路由，订单列表改为点击进入详情；详情展示商品快照、金额、地址、备注、支付记录和操作时间线。
- 统一支付时间窗：订单创建后 30 分钟内创建支付单，已创建支付单最长等待至第 35 分钟。
- 支付创建开始时订单进入 `PAYING`；渠道创建失败恢复 `UNPAID`，允许期限内使用新幂等键重试。
- 超时订单关闭并释放库存；迟到支付不恢复订单，自动进入 `REFUNDING -> REFUNDED` 并创建 `LATE_PAYMENT_REFUND` 补偿任务。
- 增加支付结果准备事务，保证迟到支付状态与补偿任务同时提交，避免留下无重试任务的待退款状态。
- 新增会员支付记录接口，并在 SQL 层校验 `orderId + memberId` 归属；对外清除支付幂等键。
- 新增 V2.8 迁移与可逆回滚脚本，支付状态约束加入 `REFUNDING`、`REFUNDED`。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 后端全量测试 | 65 项通过，0 失败 |
| 用户端生产构建 | 通过 |
| 后端完整打包 | 通过 |
| API 全链路 | 17 项通过，覆盖独立详情、`PAYING`、支付记录归属和正常支付 |
| 迟到支付集成 | `CLOSED + REFUNDED`，库存已释放，补偿任务唯一存在 |
| V2.8 迁移 | 空库升级、回滚、重新升级均通过 |
| Edge 视觉验收 | 1440px 与 390px 无横向溢出、控制台错误或图片失败 |
| 超时任务回归 | 3 笔过期订单均自动关闭，3 条锁定库存均释放，待付款计数归零后可再次下单 |

### 模块边界

- 本模块不包含物流、个人资料、安全设备管理、退款风控或角色权限开发。
- V0.3 后续继续一个模块一个提交；角色权限留到 V0.4。

### 后续修复：订单超时任务注册（2026-07-29）

- 发现 `MallOrderTimeoutTask` 已存在但未写入 `sys_job`，导致超时关闭服务不会被 Quartz 触发。
- 新增 `V2.8.1__mall_order_timeout_job.sql`，以幂等方式注册每分钟一次的关闭任务；新增对应回滚脚本。
- 实测 3 笔过期未支付订单全部变为 `CLOSED`，库存预占全部 `RELEASED`，会员待付款订单数从 3 归零，随后允许再次创建订单。
- 订单列表与详情对超时关闭显示“支付超时已关闭”，不与会员主动取消混淆。

## 商城 V0.3：模块 2 手工物流与收货（2026-07-30）

### 已完成

- 手工物流 Adapter 接收后台填写的物流公司编码、名称和运单号；一单一物流单。
- 后台新增物流履约页面，接入若依 RBAC 权限，支持订单筛选、发货、查询物流和追加节点。
- 物流节点使用固定枚举，轨迹只追加，事件时间不得早于上一节点；已签收节点更新物流单状态。
- 用户端订单详情展示物流轨迹，已签收订单支持确认收货。
- 新增 Quartz 自动确认任务，签收 7 天后订单自动进入已完成。
- 新增 V2.9.0 迁移与回滚脚本，脚本通过回滚、升级、重复升级验证。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 后端全量测试 | 75 项通过，0 失败 |
| 用户端生产构建 | 通过 |
| 若依后台生产构建 | 通过，保留既有 3 项警告 |
| Mapper XML 解析 | 物流和订单 XML 通过 |
| V2.9.0 迁移 | 回滚、升级、重复升级通过 |
| 后端健康检查 | /api/mall/health 返回 code=200、status=UP |

## 商城 V0.3：模块 3 订单项售后（2026-07-30）

### 已完成

- 新增 `mall_item_after_sale` 与 `mall_item_after_sale_item`，订单项快照、申请数量、退款金额、退货物流、风险信号和退款流水独立持久化。
- 会员接口支持申请、列表、详情和退货物流；申请按订单行锁串行校验数量占用和售后期限。
- 仅整单全数量申请退还运费；退款金额完全由服务端订单项快照计算。
- 质量问题需要凭证，但全量质量退货不产生恶意信号；其他申请仅提供人工复核提示，不自动拒绝。
- 退货物流复用物流公司白名单，商城端使用固定下拉，未知编码和 `MOCK` 均不允许会员提交。
- 若依运营端新增订单项售后页面与 `list/query/audit` 权限，审核通过、驳回和退款均写若依操作日志。
- 退款状态机拆分事务内准备、事务外通道调用和事务内结果落库；退货退款在提交物流前禁止退款。
- 修复 `MallOrderMapper.selectByIdForUpdate` 未查询 `pay_time`，导致真实数据库售后期限无法判断的问题。
- V2.10.0 菜单插入改用 UTF-8 十六进制转换，兼容客户端连接字符集并支持幂等重放。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 后端全量测试 | 85 项通过，0 失败 |
| 用户端生产构建 | 通过 |
| 若依后台生产构建 | 通过，保留既有 3 项警告 |
| 后端完整打包 | 通过 |
| 既有交易 API 回归 | 17 项通过 |
| 售后真实链路 | 申请、审核、退货物流、退款成功及驳回通过 |
| 数量边界 | 重复/超额占用返回业务拒绝 |
| V2.10 迁移 | 回滚、升级、重复升级通过；2 张表、3 条菜单、权限无重复 |
| Edge 页面检查 | 商城/若依 1440px 与 390px 无页面溢出、控制台错误或请求失败 |

### 后续入口

模块 3 完成后暂停，等待用户确认进入模块 4“个人资料与头像”。

## 商城 V0.3：模块 4 个人资料与头像（2026-07-31）

### 已完成

- 新增昵称规则：2 至 20 个 Unicode 字符、空格规范化、手机号/邮箱/网址/QQ/VX 等联系方式拦截，以及配置化敏感词过滤。
- 昵称修改按会员行锁串行，30 天最多 3 次；保存相同昵称不产生审计、不消耗次数，资料接口返回剩余修改次数。
- 新增 6 个后端白名单预设头像和自定义上传；上传同时校验扩展名、MIME、真实文件格式、文件大小、图片尺寸和像素数。
- 上传图片经服务端中心裁剪、统一输出 512×512 PNG，并清除原文件名、元数据和尾部内容。
- 自定义头像使用随机文件名并按会员目录隔离；数据库失败或事务回滚时回收新文件，事务提交后清理旧上传头像。
- 新增 `mall_member_profile_audit` 只追加审计表，昵称/头像变更记录前后值、来源、请求 IP 和时间；审计写入失败会回滚资料修改。
- 商城个人中心新增昵称编辑、剩余次数、预设头像和上传裁剪；裁剪支持缩放、水平与垂直定位，移动端打开编辑器后自动滚动至可操作区域。
- Vite 开发代理增加 `/profile`，上传头像可通过商城开发端直接显示。
- 新增 `V2.11.0__mall_member_profile.sql` 及回滚脚本。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 后端全量测试 | 104 项通过，0 失败 |
| 用户端生产构建 | 通过 |
| 后端完整打包 | 通过 |
| 最终 JAR 健康检查 | 8080 返回 `code=200`、`status=UP` |
| V2.11.0 迁移 | 升级、回滚、再次升级、重复升级通过；最终审计表 1 张 |
| 昵称真实链路 | 3 次修改成功，第 4 次拒绝；最终 JAR 敏感内容拒绝通过 |
| 头像真实链路 | 预设头像、JPEG 上传、512×512 PNG 输出、8080/5174 图片访问通过 |
| 审计 | 测试会员审计 `NICKNAME=3`、`AVATAR=2` |
| Edge 页面检查 | 商城 1440px 与 390px 无溢出、裁切、控制台错误、请求失败或图片失败 |

### 后续入口

模块 4 独立收尾后进入模块 5“会员会话与设备安全”。模块 5 先确认设备标识、设备类型判定、主移动设备替换和短信验证状态机，再实施数据库迁移；不在本模块提前修改角色权限或商城整体 UI。

## 缺陷修复：订单列表售后进度（2026-07-31）

### 根因与修复

- 订单项售后使用独立状态，但原订单列表把前端 `AFTER_SALE` 参数直接作为 `mall_order.status` 查询；因此 `SHIPPED + APPROVED` 等有效组合被“售后中”标签漏掉。
- 保留订单履约状态，不在部分售后时修改订单主状态。
- 新增活跃售后订单查询，兼容旧整单 `AFTER_SALE` 状态，并按活跃售后计算列表 `displayStatus`。
- 列表返回 `activeAfterSaleCount`、`latestAfterSaleId`、`latestAfterSaleType`、`latestAfterSaleStatus` 和最新更新时间。
- “已发货/已完成”筛选排除活跃售后订单，售后结束或驳回后自然恢复原履约分类。
- 商城订单卡片按售后状态显示待审核、等待寄回商品、退货运输中或退款处理中。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 定向回归 | `MallOrderQueryServiceTest` 4 项通过；新增测试先失败后通过 |
| 后端全量测试 | 105 项通过，0 失败 |
| 商城端生产构建 | 通过 |
| 后端完整打包与健康检查 | 通过，8080 返回 `status=UP` |
| 原始 API 复现 | 活跃售后 1 条、售后中订单 1 条、缺失订单 0 条 |
| 分类互斥 | 订单 40 不在“已发货”，在“全部”与“售后中”各正确显示 |
| Edge 页面检查 | 1440px/390px 显示“等待寄回商品”，无溢出、裁切、控制台错误或失败请求 |

## 商城 V0.3：模块 5 会员会话与设备安全（2026-07-31）

### 已完成

- 新增 `mall_member_device`、`mall_member_session`、`mall_member_device_audit`，分离设备归属、在线会话和主设备更换审计。
- 浏览器随机设备标识持久化在 `localStorage`；后端根据 User-Agent 判定移动端/电脑端，平板计入移动端。
- 会话策略限制为 2 台移动端和 1 台电脑端；覆盖同设备 Token 轮换、电脑替换、第三台手机保留主设备并淘汰普通手机。
- 首台手机登记为主移动设备，离线后主身份保留；更换主设备使用独立短信验证码并限制 30 天 3 次。
- Token 在数据库和 Redis 键中均使用 SHA-256 摘要，远程下线、退出和替换只在事务提交后清理缓存。
- 新增设备概览、远程下线、主设备验证码和主设备更换接口；个人中心完成对应真实交互。
- 移动端主设备验证表单打开后自动滚动至固定底部导航上方。
- 新增 `V2.12.0__mall_member_device_session.sql` 及回滚脚本。

### 验证记录

| 项目 | 结果 |
| --- | --- |
| 后端全量测试 | 123 项通过，0 失败 |
| 用户端生产构建 | 通过 |
| 后端完整打包与健康检查 | 通过，8080 返回 `status=UP` |
| V2.12.0 迁移 | 升级 3 张、回滚 0 张、再次升级和重复升级 3 张 |
| 多设备真实链路 | 3 手机 + 2 电脑后在线 2 手机 + 1 电脑，旧会话 401 |
| 主设备链路 | 独立短信验证码、更换审计、剩余次数和远程下线通过 |
| Edge 页面检查 | 1440px/390px 无溢出、遮挡、控制台错误或失败请求 |

### 后续入口

模块 5 独立收尾后进入模块 6“商城用户端 UI 升级”。模块 6 复用真实业务接口，不提前处理 V0.4 角色权限和运营端菜单信息架构。
