# V0.4 模块 4：物流履约工作台验收记录

日期：2026-08-04
分支：`dev`

## 完成内容

- 将若依原发货页面升级为物流履约工作台，使用标准 PageHelper 分页，支持订单号、收件人、物流公司、履约阶段、运营关注和下单时间筛选。
- 新增履约全局汇总、最新轨迹、发货/签收时间，以及超 24 小时未发货、运输异常、订单与物流数据不一致三类只读关注信号。
- 物流公司选项改由服务端白名单统一提供，正式运营端不展示模拟物流渠道。
- 保留一单一物流和手工 Adapter；追加节点时锁定物流单，签收后仅允许追加更正说明，更正不回退签收状态且不阻止确认收货。
- 权限沿用 `mall:logistics:list/query/ship/node`，未新增权限字符串或角色体系。

## 数据库验收

测试库：`mall_migration_test_20260728`。未修改原业务库 `ry-vue`。

- V2.16.0 升级：新增 `idx_mall_logistics_company_time(company_code,shipped_time)`、`idx_mall_logistics_status_time(status,delivered_time)`，菜单 3960 更新为“物流履约工作台”。
- 重复升级：索引不重复，菜单保持目标名称。
- 回滚：两条索引删除，菜单恢复为“发货工作台”。
- 再升级：两条索引和菜单名称恢复，最终保持升级状态。

## 测试与真实链路

- 物流定向测试：12 项通过。
- `mall-business` 全量测试：138 项通过。
- 若依前端 `npm.cmd run build:prod`：通过；仅保留项目既有钢琴接口导出和包体积警告。
- 后端完整打包：通过；8080 使用测试库正常启动，列表、汇总和物流公司接口返回 200。
- 测试库履约汇总：32 条，待发货 27、运输中 1、签收待确认 1、已完成 1、数据不一致 2；公司白名单 9 家且不含 `MOCK`。
- 真实岗位接口：运营主管、仓储履约、客服售后查询放行；商品运营、财务风控查询返回业务码 403。
- 临时已签收订单验证：追加 `CORRECTION` 返回 200，运单保持 `DELIVERED`；再追加 `IN_TRANSIT` 返回业务码 500。验收订单、物流单、节点和操作日志已清理。

## Edge 验收

- 1440×900：筛选、全局汇总、列表固定列和轨迹详情弹窗通过，无页面级横向溢出或控制台错误。
- 390×844：筛选区、两列统计带、横向表格和全屏发货弹窗通过，字段与按钮无截断，无页面级横向溢出或控制台错误。

## 运行地址

- 商城用户端：`http://127.0.0.1:5174`
- 若依运营端：`http://127.0.0.1:8081`
- 后端：`http://127.0.0.1:8080`

服务使用测试库、Redis `6379` 和 `MALL_SMS_MOCK_CODE=123456` 启动；测试库图形验证码配置已恢复为开启。
