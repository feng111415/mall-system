# 商城系统数据库迁移验证

执行日期：2026-07-28

## 已确认

- Windows 服务 `MySQL84` 状态为 `Running`。
- 本机 `127.0.0.1:3306` 端口可连接。
- MySQL 客户端存在：`C:\Program Files\MySQL\MySQL Server 8.4\bin\mysql.exe`。
- 迁移脚本按版本顺序可识别到 `V2.6.0`。

## 验证结果

使用通过安全环境变量注入的 MySQL 测试凭据连接成功，并在独立测试库 `mall_migration_test_20260728` 完成迁移：

- 基础若依脚本 `ry_20260417.sql`：通过。
- Quartz 脚本 `quartz.sql`：通过。
- 商城版本脚本执行至 `V2.6.0`：通过。
- 商城表数量：26 张。
- 对账权限数量：5 条，菜单 ID 为 `3900-3904`。
- 原业务库 `ry-vue`：未修改。

## 测试库

测试库保留为 `mall_migration_test_20260728`，可使用 Navicat 查看。后续如需清理，仅删除该测试库即可，不要删除 `ry-vue`。

## V2.13.0 运营后台信息架构迁移（2026-08-03）

- 在 `mall_migration_test_20260728` 依次执行升级、重复升级、回滚、再次升级，全部通过；最终保持 V2.13.0 已升级状态。
- 新增菜单/权限节点 6 条：运营职责 1 条、业务域目录 4 条、售后退款权限 1 条；菜单 ID、角色键均无重复。
- 新建商城运营角色 5 条，菜单授权数分别为：运营主管 40、商品运营 19、客服售后 13、仓储履约 11、财务风控 11。
- 五类角色的非商城菜单授权均为 0；客服拥有审核但无退款，财务拥有退款但无审核，仓储拥有库存调整和发货，商品运营没有库存调整。
- `sys_role_menu` 无悬空角色或菜单引用；原功能菜单 ID、组件和权限标识未改变，回滚后原父级、顺序和名称恢复。
- 原业务库 `ry-vue` 未修改。
