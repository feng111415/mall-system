# 商城系统数据库迁移验证

执行日期：2026-07-28

## 已确认

- Windows 服务 `MySQL84` 状态为 `Running`。
- 本机 `127.0.0.1:3306` 端口可连接。
- MySQL 客户端存在：`C:\Program Files\MySQL\MySQL Server 8.4\bin\mysql.exe`。
- 迁移脚本按版本顺序可识别到 `V2.6.0`。

## 阻塞

使用项目文档和配置中的 `root/123456` 连接测试库失败：

```text
ERROR 1045 (28000): Access denied for user 'root'@'localhost'
```

使用 root 空密码同样失败。当前没有得到可用的数据库账号，因此没有创建测试库、没有执行任何迁移脚本，也没有修改数据库密码或业务数据。

## 待继续

提供一个可用的 MySQL 测试账号后，再按以下顺序执行：

1. 创建独立测试库。
2. 导入 `ry_20260417.sql` 和 `quartz.sql`。
3. 按版本顺序执行商城迁移脚本至 `V2.6.0`。
4. 校验商城表、字段、索引、约束和菜单权限。
5. 输出迁移结果并删除测试库或保留供验收。
