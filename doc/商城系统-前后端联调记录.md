# 商城系统前后端联调记录

执行日期：2026-07-28

## 联调环境

- 数据库：`mall_migration_test_20260728`
- MySQL：`127.0.0.1:3306`
- Redis：`127.0.0.1:6379`
- 后端验证端口：`18080`，默认代理端口：`8080`
- Vue 开发端口：`18081`

## 已通过

- 后端可连接测试库并完成 Spring 容器启动。
- `GET http://127.0.0.1:18080/api/mall/health` 返回 HTTP 200。
- Vue 开发服务器编译成功，访问 `http://127.0.0.1:18081/`。
- Vue `/dev-api` 代理到后端 `8080` 成功：
  `GET http://127.0.0.1:18081/dev-api/api/mall/health` 返回 HTTP 200。

## 本轮修复

- 订单域与支付域同名 `MallPaymentStatus` 导致 MyBatis 类型别名冲突，订单域改为 `MallOrderPaymentStatus`。
- `MallOrderCreateService` 两个构造器同时标记 `@Autowired`，保留生产主构造器自动注入。
- 对账菜单迁移 ID 与既有钢琴菜单冲突，调整为 `3900-3904` 并验证 5 条权限均落库。

## 临时进程

联调使用的后端、前端和 Redis 是临时进程。结束联调时应停止对应进程；数据库测试库 `mall_migration_test_20260728` 可保留供验收，原库 `ry-vue` 未修改。
