# 商城系统开发进度

更新时间：2026-07-27

## 当前版本

- 分支：`dev`
- 最新版本：`v2.1.0`
- 最新提交：`ef8c0fa feat: 完成订单操作审计查询`
- 远程仓库：`https://github.com/feng111415/mall-system.git`

## 已完成模块

- `v0.1.0` 工程基线与商城骨架
- `v0.2.0` 会员、短信验证码、商品中心
- `v0.3.0` 库存中心
- `v0.4.0` 购物车
- `v0.5.0` 订单创建、商品快照、订单号
- `v0.6.0` 库存预占
- `v0.7.0` 订单取消、超时关单、Quartz 入口
- `v0.8.0` 模拟支付与库存确认
- `v0.9.0` 会员订单列表、详情和订单中心
- `v1.0.0` 物流履约、后台发货、用户物流轨迹查询
- `v1.1.0` 售后退款申请、审核、Mock 退款和状态回滚
- `v2.0.0` 基础交易风控、待付款订单上限、风险记录
- `v2.1.0` 订单操作日志后台分页查询与筛选

## 最近验证

- 后端全量测试：47 项通过
- 用户端 Vite 构建：通过
- `ruoyi-admin` 打包：通过
- MyBatis Mapper XML：全部解析通过
- `ai-web/` 保持未跟踪，未提交

## 明天继续

进入治理模块第二步：

1. 设计库存、支付、退款异常补偿记录。
2. 增加补偿任务的幂等执行和失败重试。
3. 增加后台补偿记录查询和手工重试接口。
4. 补充测试、构建、Git 提交和版本标签。

## 数据库脚本注意

按版本顺序执行 `sql/` 下商城迁移脚本。`V2.0.0__mall_risk_governance.sql` 会为 `mall_order` 增加 `risk_status` 字段，执行前确认目标数据库尚未重复执行该迁移。
