# 商城系统项目交接总览

> 新会话开始时，先完整阅读本文件和 `CONTEXT.md`，再开始任何修改。
>
> 最后更新：2026-08-05。商城 V0.1 至 V0.4 已完成；V0.5 模块 1“首页内容运营”已完成规则与静态模型确认，正式实现尚未开始。

## 1. 项目状态

这是基于 RuoYi-Vue 的前后端分离单商户 B2C 商城测试项目。后台运营端复用若依能力，商城用户端是独立 Vue 3 应用，商城业务后端以独立模块接入若依工程。

当前已完成：

- **V0.1**：会员、商品、库存、购物车、地址、结算、订单、Mock 支付、履约售后、风控治理、后台运营和数据库迁移。
- **V0.2**：用户端商品浏览、全局框架、商品详情与加购、购物车结算、个人中心地址簿、最终视觉验收和完整回归。
- **V0.3 模块 1**：独立订单详情页、支付尝试记录、订单创建后 30 分钟支付单创建期限、35 分钟支付结果期限、超时关闭及迟到支付自动原路退款。
- **V0.3 模块 2**：若依运营后台手工发货、追加式物流轨迹、会员查看物流与确认收货、签收 7 天自动确认收货。
- **V0.3 模块 3**：订单项级仅退款/退货退款、数量占用与期限校验、服务端退款金额、退货物流、若依审核和退款状态机。
- **V0.3 模块 4**：昵称规则与 30 天修改次数限制、预设/上传头像、安全裁剪重编码、资料修改审计。
- **V0.3 模块 5**：随机浏览器设备标识、2 台移动端与 1 台电脑端在线限制、主移动设备保护、短信验证替换及设备下线。
- **V0.3 模块 6**：以 `prototypes/mall-v0.3-visual-prototype/preview-c.png` 为唯一视觉基准，按方案 C 重做商城用户端展示层，并完成全站视觉统一、桌面反馈、移动端弱动态和减少动态效果支持。
- **V0.3 基础加固**：支付退款幂等、外部 I/O 事务拆分、并发下单上限、短信失败计数持久化，以及商城会员 Token 与若依管理员 Token 隔离。
- **V0.4 模块 1**：按 A“业务域分组”重构现有若依商城菜单，建立五类运营岗位和真实 RBAC 权限边界，并拆分售后审核与退款执行权限。
- **V0.4 模块 2**：新增若依订单运营中心，统一查询订单并聚合商品、支付、库存、物流、售后、风控与操作记录，保留各业务模块的写操作边界。
- **V0.4 模块 3 至模块 5**：商品与库存运营中心、物流履约工作台、售后资金与异常处理中心及岗位职责下发已完成。
- **V0.4 模块 6**：会员运营与登录安全已完成，风险触发式拼图验证已接入商城登录页。
- **V0.4 模块 7**：运营工作台与全局验收已完成，五类岗位真实接口及 Edge 桌面/移动端验收通过。
- **V0.5 模块 1**：首页内容运营已确认业务范围和静态模型，最终采用 B 内容清单作为管理首页、A 预览工作台作为新增/编辑页；下一步开始正式实现。

## 2. 仓库与运行环境

| 项目项 | 当前值 |
| --- | --- |
| 仓库目录 | `C:\Users\Administrator\Desktop\RuoYiWork\RuoYi-Vue-master` |
| Git 分支 | `dev` |
| 远程仓库 | `https://github.com/feng111415/mall-system.git` |
| 最新完成阶段 | V0.5 模块 1：规则与静态模型确认 |
| 商城用户端 | `http://localhost:5174` |
| 若依运营端 | `http://localhost:8081` |
| 商城后端 | `http://localhost:8080` |
| Redis | `127.0.0.1:6379` |
| MySQL | `127.0.0.1:3306`，Windows 服务 `MySQL84` |
| 测试数据库 | `mall_migration_test_20260728` |
| Mock 短信验证码 | `123456` |

本轮结束时，`5174`、`6379`、`8080`、`8081` 已在本机运行。正式运营端为 `ruoyi-ui` 的 `8081`；`18080` 和 `18081` 是历史临时后台联调端口。

### 当前工作区规则

- `ai-web/` 是用户的未跟踪目录，必须保留；禁止提交、删除、移动或重构。
- 不修改原业务库 `ry-vue`；所有迁移、联调和验收只使用 `mall_migration_test_20260728`。
- 当前是测试项目，不打包 Docker、不发布、不执行生产部署。
- 后续项目改动完成验证后，必须提交并推送 GitHub `dev`。
- 不执行 `git reset --hard`、`git checkout --`、清理未跟踪目录等破坏性操作。

## 3. 架构与目录

```text
mall-storefront (Vue 3 + Vite + Pinia + Vue Router + Vant)
       |
       | /api/mall/**，商城独立 Bearer Token
       v
mall-business (Spring Boot 商城模块)
       |
       +-- Controller -> Service -> Mapper -> MySQL
       +-- Redis（验证码、缓存、Token）
       +-- 复用 RuoYi 框架、安全、日志、任务等通用能力

ruoyi-ui / ruoyi-admin (若依后台运营端)
```

关键目录：

```text
mall-storefront/                         商城用户端
mall-business/                           商城业务后端
ruoyi-admin/                             后端启动模块
sql/                                     版本化数据库迁移
scripts/mall-storefront-e2e.ps1          商城 API 全链路验收
doc/                                     需求、架构、验收、开发记录
doc/architecture/                        架构图与设计资料
```

核心文档：

```text
CONTEXT.md                               领域术语和统一语言
doc/商城系统-需求分析说明书.md             需求基线
doc/商城系统-架构设计说明书.md             架构说明和图
doc/商城系统-订单状态设计.md               订单状态机
doc/商城系统-开发进度.md                   简版进度
doc/development/商城开发记录.md             历史开发记录
doc/development/mall-v0.2-api-contract.md V0.2 商品查询契约
```

## 4. 不可突破的开发规则

### 后端与数据

- 固定分层：`Controller -> Service -> Mapper`；Controller 不写业务规则或 SQL。
- 商城用户端接口统一 `/api/mall/**`，返回统一使用 `AjaxResult`。
- 会员使用商城独立 Bearer Token，绝不混用若依后台管理员 Token。
- 写操作使用 DTO、`@Valid` 和事务；Controller 不得绕过 DTO。
- 数据库变更只能使用版本化 SQL 迁移，不能手工改测试库结构。
- 客户端排序字段必须映射到服务端白名单，禁止原样拼接进 SQL。
- 地址查询、修改、删除、设默认和下单必须按当前会员校验资源归属。
- 下单与支付必须保留幂等控制、库存二次校验和防超卖逻辑。

### 安全与治理

- 短信验证码需要保持手机号校验、过期、重发间隔、日限额和错误次数限制。
- Token 不得出现在 URL、日志或响应中。
- 关键写操作保留操作日志；版本通过 Git 分支、提交和发布脚本管理。
- 后台权限使用 RuoYi RBAC；商城会员权限与管理员权限严格隔离。

### 前端

- 延续 Vue 3、Vite、Vue Router、Pinia、Vant 的既有组织方式，不引入冲突的请求或状态方案。
- 新请求集中在 `src/api/`，全局状态集中在 Store，页面不散落底层请求实现。
- 保持简洁生活方式商城风格，覆盖加载、空状态、错误反馈、交互忙碌态和移动端体验。
- 桌面与移动端都必须验收；移动端五栏底部导航可用，页面不得横向溢出。

## 5. V0.1 已完成能力

- 手机号 + 短信验证码注册登录，开发环境固定验证码为 `123456`。
- 独立会员 Token、会员资料、协议同意和收货地址。
- 分类、品牌、SPU、SKU、库存快照、库存预占和库存流水。
- 购物车、结算预览、订单创建、订单快照、订单幂等和 Mock 支付。
- 订单列表、物流履约、售后退款、基础风控、补偿任务、支付退款对账与告警。
- 若依运营后台、RBAC 菜单权限、操作审计、版本化数据库迁移和全链路验收脚本。

## 6. V0.2 完成清单

| 序号 | 阶段 | 状态 | 结果 |
| --- | --- | --- | --- |
| 1 | 接口与数据基线 | 已完成 | 商品契约、8 个 SPU、17 个 SKU、17 条库存快照 |
| 2 | 商品浏览 | 已完成 | 首页真实商品、分类、搜索、排序、骨架和空状态 |
| 3 | 全局框架 | 已完成 | 桌面导航、移动五栏、全局通知、购物车角标同步 |
| 4 | 商品详情与加购 | 已完成并验收 | SKU、数量、库存、加购反馈、相关推荐和最近浏览 |
| 5 | 购物车与结算体验 | 已完成并验收 | 全选、数量金额、失效处理、地址、备注、结算进度和支付结果 |
| 6 | 个人中心与地址簿 | 已完成并验收 | 地址编辑、删除、默认切换、表单反馈和移动端交互 |
| 7 | 视觉验收、回归、提交 | 已完成 | 构建、53 项后端测试、14 项 API、14 个浏览器视图和 GitHub 同步 |

### 商品与浏览

- 测试库含 8 个已上架 SPU、17 个有效 SKU、17 条库存快照，覆盖家居、数码、穿搭、咖啡和出行。
- `GET /api/mall/catalog/products` 支持 `categoryId`、`keyword`、`sort`；排序只允许 `default`、`sales`、`priceAsc`、`priceDesc`。
- 首页显示全部 8 个真实商品；商品列表提供分类、关键词、销量与价格排序、加载骨架和空状态。

### 商品详情、购物车与结算

- 商品详情默认选中可售 SKU；切换 SKU 后价格与库存同步；售罄 SKU 禁止加购；数量受库存边界限制。
- 详情页安全清理 `detailHtml` 后渲染，提供相关推荐与最近浏览。
- 购物车支持全选、数量调整、删除、刷新、失效清理、库存提示和粘性结算栏。
- 结算页提供地址选择、费用明细、订单备注（最多 200 字）、确认订单、模拟支付和订单入口。
- 订单备注已经由订单详情接口确认落库；重复提交与重复支付受幂等控制。

### 个人中心与地址簿

- 会员 API 已接入地址新增、查询、修改、删除；后端持续按 `addressId + memberId` 校验归属。
- 个人中心提供新增/编辑共用表单、省市区级联、收货人/手机号/地区/详细地址字段校验和错误反馈。
- 地址卡片支持设为默认、编辑、页面内删除确认、忙碌态及全局成功/失败通知。
- 地址编辑、设默认、删除后会刷新列表和地址数量；移动端操作区改为单列布局。

## 7. 最近完整验证

| 验证项 | 命令或方式 | 结果 |
| --- | --- | --- |
| 后端全量测试 | `mvn -pl mall-business -am test` | 53 项通过，0 失败 |
| 用户端生产构建 | `npm.cmd run build`（`mall-storefront`） | 通过 |
| API 全链路 | `powershell -ExecutionPolicy Bypass -File scripts/mall-storefront-e2e.ps1` | 14 项通过 |
| 真实 Edge 交互 | 登录、加购、购物车、结算、支付、地址簿 | 通过 |
| 最终视觉审查 | 7 条核心路由，1440px + 390px | 14 个视图通过，无控制台错误、无图片加载失败、无横向溢出 |

最终视觉审查覆盖：`/`、`/catalog`、`/product/1`、`/cart`、`/checkout`、`/orders`、`/account`。两种视口均满足 `scrollWidth == clientWidth`。

验证只写入独立测试库 `mall_migration_test_20260728`；原库 `ry-vue` 未修改。

## 8. V0.3 模块 1：订单详情与支付时限

- 用户端新增独立路由 `/orders/:orderId`；订单列表点击进入详情，展示商品快照、收货信息、费用、支付记录和操作时间线。
- 订单详情返回 `paymentCreateDeadline`、`paymentResultDeadline`、`canCreatePayment`、`canConfirmPayment`、`canCancel` 和 `payTime`。
- 支付单必须在订单创建后 30 分钟内创建；创建期间订单进入 `PAYING`，渠道创建失败恢复 `UNPAID`。
- 无支付单订单到第 30 分钟关闭；已创建支付单订单等待到第 35 分钟，再关闭支付单与订单并释放库存。
- 关闭后收到支付成功结果时，订单保持 `CLOSED`，支付进入 `REFUNDING -> REFUNDED`；补偿任务与状态变更同事务持久化，再尝试即时原路退款。
- 新增会员接口 `GET /api/mall/orders/{orderId}/payments`，SQL 同时校验订单和支付记录的会员归属，返回前清除幂等键。
- 数据库版本为 `V2.8.0__mall_payment_deadline.sql` 和 `V2.8.1__mall_order_timeout_job.sql`；对应回滚脚本位于 `sql/rollback/`。
- V2.8.1 注册每分钟执行一次的 Quartz 订单超时任务。超时订单保持 `CLOSED` 终态并展示“支付超时已关闭”，释放库存后不会计入每会员最多 3 笔进行中待付款订单。

本模块验收结果：后端 65 项单元测试通过，用户端生产构建通过，API 全链路 17 项通过；Edge 1440px/390px 订单详情无横向溢出、控制台错误或图片失败；V2.8 空库升级、回滚、再升级通过；迟到支付集成验证确认订单关闭、支付退款、库存释放和补偿任务一致。

## 9. 环境启动与停止

### 启动前检查

```powershell
Get-Service MySQL84
Get-NetTCPConnection -State Listen -LocalPort 5174,6379,8080
```

### Redis

```powershell
Set-Location C:\Users\Administrator\Desktop\RuoYiWork\tools\redis
.\redis-server.exe --bind 127.0.0.1 --port 6379 --protected-mode yes
```

### 商城后端

后端必须显式连接测试库，禁止使用配置文件默认的 `ry-vue`：

```powershell
Set-Location C:\Users\Administrator\Desktop\RuoYiWork\RuoYi-Vue-master
$env:RUOYI_DATASOURCE_URL='jdbc:mysql://127.0.0.1:3306/mall_migration_test_20260728?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai'
$env:RUOYI_DATASOURCE_USERNAME='root'
$env:RUOYI_DATASOURCE_PASSWORD='<从安全凭据存储注入>'
$env:MALL_SMS_MOCK_CODE='123456'
java -Xms256m -Xmx1024m -jar ruoyi-admin\target\ruoyi-admin.jar --server.port=8080
```

健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8080/api/mall/health
```

### 商城用户端

```powershell
Set-Location C:\Users\Administrator\Desktop\RuoYiWork\RuoYi-Vue-master\mall-storefront
npm.cmd run dev -- --host 0.0.0.0
```

访问：`http://localhost:5174/`。

停止临时进程时，只停止对应 Java、Node 或 Redis 进程；不要停止 MySQL84 服务、删除测试库或清理 `ai-web/`。

## 10. 新会话入口

新会话可直接发送：

```text
请先读取 C:\Users\Administrator\Desktop\RuoYiWork\RuoYi-Vue-master\PROJECT_HANDOFF.md 和 CONTEXT.md。商城 V0.1、V0.2、V0.3 六个模块及基础加固已完成并通过回归，下一阶段是 V0.4 运营端信息架构调整。先确认物流履约和库存管理归入会员管理的菜单、路由、授权兼容及迁移边界，再按模块规则开发。保留 ai-web/ 和 prototypes/mall-v0.3-visual-prototype/，不要修改 ry-vue，不要清理工作区，也不要擅自发布或打包 Docker。
```

## 11. 2026-07-30 完成记录：V0.3 模块 2 手工物流与收货

V0.3 模块 2 已完成并通过回归。正式运营端统一使用 `ruoyi-ui` 若依后台，独立 `mall-admin` 不再作为正式运营端。

- 完成一单一物流单、后台手工发货、固定节点追加：已发货、运输中、派送中、已签收、运输异常、更正说明。
- 追加节点服务端校验时间顺序；已签收后禁止继续追加；关键后台写操作写入订单操作审计日志。
- 会员可查询本人物流并主动确认收货；签收 7 天后 Quartz 自动确认收货。
- 若依运营端新增物流履约菜单和接口：`GET /mall/logistics/orders`、`POST /mall/logistics/orders/{orderId}/ship`、`GET /mall/logistics/shipments/{shipmentId}`、`POST /mall/logistics/shipments/{shipmentId}/nodes`。
- 用户端订单详情展示物流时间线和确认收货入口。
- 数据库迁移为 `sql/V2.9.0__mall_manual_logistics.sql`，回滚脚本为 `sql/rollback/V2.9.0__mall_manual_logistics_rollback.sql`。

验证结果：`mvn -pl mall-business -am test` 75 项通过；`mvn -pl ruoyi-admin -am package -DskipTests` 通过；商城用户端构建通过；若依开发端 `http://localhost:8081` 编译成功；后端 `http://localhost:8080/api/mall/health` 返回 `code=200/status=UP`；V2.9.0 在 `mall_migration_test_20260728` 完成回滚、升级、重复升级验证，Quartz 任务保持 1 条、物流权限保持 4 条。

补充修复：V2.9.0 菜单名称曾因使用错误字符集执行迁移显示为 `????`。迁移脚本已增加基于 UTF-8 字节值的幂等修复；测试库 3960-3963 已恢复为“物流履约、物流查询、订单发货、追加轨迹”。若依后台刷新页面即可看到正确名称。

物流公司约束补充：手工发货已改为固定物流公司下拉选择；后端同步使用白名单校验并按编码反查公司名称，忽略客户端自带的公司名称，防止自由填写或前后端名称不一致。当前支持顺丰速运、中通快递、圆通速递、申通快递、韵达速递、京东物流、中国邮政 EMS、极兔速递、德邦快递；`MOCK` 仅保留测试用途，不在正式下拉框展示。

## V0.4 模块 1：运营端信息架构与职责权限

用户最终确认 A“业务域分组”，正式功能建设在现有 `ruoyi-ui + ruoyi-admin + mall-business`，不建立第二套运营后台。

- `商城运营` 下新增“运营职责、商品与库存、订单与履约、售后与资金、会员运营”；保留原功能菜单 ID、路由、组件和权限标识。
- 新增运营主管、商品运营、客服售后、仓储履约、财务风控五类若依角色；所有岗位只获得商城权限，不获得若依系统级权限。
- 新增“运营职责”正式页面，展示当前登录身份、岗位职责、禁止越权边界和与实际授权一致的权限矩阵。
- 售后审核继续使用 `mall:after-sale:audit`，退款执行拆分为 `mall:after-sale:refund`；客服不能退款，财务不能审核。
- 迁移为 `V2.13.0__mall_operations_information_architecture.sql`，已完成升级、重复升级、回滚和再次升级；测试库最终保持升级状态。
- 下一模块进入“订单运营中心”，补齐统一订单查询、详情和跨支付/库存/履约/售后的运营入口。

## 12. 2026-07-30 完成记录：V0.3 基础加固

在继续 V0.3 UI、物流和个人中心之前，已完成架构审查发现的高风险问题加固。代码、自动化测试、真实链路验证和收尾复查均已完成，本次随交接文档一并提交到 `dev`。

- 支付与退款 Port 增加稳定的支付单号/退款单号幂等键；Mock Adapter 对相同键返回稳定的通道流水号。
- 售后退款和异常支付退款拆分为“事务内准备 -> 通道调用 -> 事务内落库”，外部 I/O 不再占用订单、支付或退款行锁；通道结果不完整时保留 `REFUNDING` 以便使用同一幂等键安全重试。
- 创建订单前锁定会员行后再统计进行中未支付订单，保证“最多 3 笔”在并发下单时为原子约束。
- 短信验证码错误次数迁移到 `REQUIRES_NEW` 事务，避免登录异常回滚失败计数。
- 商城前后端改用 `X-Mall-Authorization`；若依后台仍使用 `Authorization`，两种 Token 不再被同一 JWT 过滤器混淆。
- 新增状态持久化 module：`MallRefundApprovalStateService`、`MallLatePaymentRefundStateService`、`MallSmsVerificationAttemptService`。

已完成验证：

- `mvn -pl mall-business -am test`：67 项通过。
- `npm.cmd run build`（`mall-storefront`）：通过。
- `mvn -pl ruoyi-admin -am package -DskipTests`：通过。
- `/api/mall/health` 健康检查通过；真实登录、下单、支付与退款链路已使用 `X-Mall-Authorization` 验证通过。
- 收尾差异检查通过；`ai-web/`、`prototypes/mall-v0.3-visual-prototype/` 及用户私人会话资料均未纳入提交。

## 13. 2026-07-30 完成记录：V0.3 模块 3 订单项售后

V0.3 模块 3 已完成并通过回归。售后由原整单退款扩展为订单项级流程，正式运营端继续使用 `ruoyi-ui` 若依后台。

- 会员可针对已发货或已完成的已支付订单申请仅退款、退货退款；支持多订单项和每项数量。
- 服务端按订单项快照计算商品退款金额；仅整单全数量申请时退运费，前端金额不参与计算。
- 对超数量、活跃售后重复占用和超过支付后 7 天期限执行硬拦截；质量问题必须提供凭证，但全数量质量退货不标记为恶意。
- 退货物流只接受顺丰、中通、圆通、申通、韵达、京东物流、EMS、极兔和德邦固定编码；商城端使用下拉选择。
- 若依运营端支持列表、详情、审核通过、驳回和退款；退货退款必须先审核、由会员提交物流，再由运营确认收到退货并退款。
- 退款保持“事务内准备 -> 通道调用 -> 事务内落库”，以售后单号作为稳定退款幂等键；通道结果不完整时保留 `REFUNDING` 供安全重试。
- 新增迁移 `sql/V2.10.0__mall_item_after_sale.sql` 和回滚脚本，创建 `mall_item_after_sale`、`mall_item_after_sale_item` 及 3 条若依菜单权限。
- 修复订单加锁查询遗漏 `pay_time` 的问题，真实数据库中的售后期限校验现在按支付时间生效。

验证结果：

- `mvn -pl mall-business -am test`：85 项通过，0 失败。
- 商城用户端与若依运营端生产构建均通过；若依保留项目既有的 3 项警告。
- 既有会员登录、商品、购物车、地址、结算、下单、支付链路 17 项通过。
- 真实订单项售后链路通过：申请退货退款 -> 审核通过 -> 提交顺丰退货物流 -> 确认收到并退款 -> `SUCCESS`；另验证重复数量拒绝和运营驳回。
- V2.10 在 `mall_migration_test_20260728` 完成回滚、升级、重复升级，最终 2 张表、3 条菜单、权限无重复；原库 `ry-vue` 未修改。
- Edge 1440px/390px 检查商城订单详情和若依售后页，均无页面横向溢出、控制台错误或请求失败；若依移动端表格可横向查看完整字段。

下一步：等待用户确认后进入 V0.3 模块 4“个人资料与头像”。

## 14. 2026-07-30 会话暂停点

- V0.3 模块 3 已提交并推送：`38e0112 feat: 完成V0.3订单项售后模块`。
- 当前 `dev` 与 `origin/dev` 一致，工作区只保留用户未跟踪的 `ai-web/`、视觉原型和会话说明。
- 明日继续时先确认服务与测试库状态，再从 V0.3 模块 4“个人资料与头像”开始设计和开发。
- 模块 4 必须独立完成迁移、后端测试、前端构建、真实链路、Edge 桌面/移动检查和 Git 提交后，才能进入模块 5。

## 15. 2026-07-31 完成记录：V0.3 模块 4 个人资料与头像

V0.3 模块 4 已完成开发、迁移、回归、真实链路和 Edge 桌面/移动端验收。

- 昵称统一清理首尾与重复空格，按 Unicode 字符限制为 2 至 20 个字符；拦截手机号、邮箱、网址、QQ/VX 等联系方式及可配置敏感词。
- 昵称 30 天最多修改 3 次，使用会员行锁保证并发请求不能绕过；保存相同昵称不消耗次数，资料接口返回剩余次数。
- 提供 6 个后端白名单预设头像；自定义头像仅接受 JPG/JPEG/PNG 和最大 5MB，同时校验扩展名、MIME、真实格式、尺寸及像素总量。
- 上传头像经服务端中心裁剪并统一重编码为 512×512 PNG，清除原文件名、元数据和尾部数据；文件使用随机名并按会员隔离存储。
- 新上传文件在数据库失败或事务回滚时回收，旧上传头像仅在事务提交后清理。
- 新增只追加的 `mall_member_profile_audit`，记录昵称/头像前后值、来源、请求 IP 和时间；审计失败会使资料变更回滚。
- 商城个人中心支持昵称编辑、剩余次数、预设头像、自定义上传及带缩放和水平/垂直定位的本地裁剪。
- 数据库迁移为 `sql/V2.11.0__mall_member_profile.sql`，回滚脚本为 `sql/rollback/V2.11.0__mall_member_profile_rollback.sql`。

验证结果：

- `mvn -pl mall-business -am test`：104 项通过，0 失败。
- `mvn -pl ruoyi-admin -am package -DskipTests`：通过；最终 JAR 已连接 `mall_migration_test_20260728` 在 8080 启动，健康检查返回 `code=200/status=UP`。
- 商城用户端生产构建通过。
- V2.11.0 在测试库完成升级、回滚、再次升级和重复升级验证，最终审计表保持 1 张。
- 真实会员完成 3 次昵称修改，第 4 次被拒绝；预设头像、JPEG 上传、审计记录和 8080/5174 图片访问均通过。
- 最终 JAR 复验“官方客服123”被敏感内容规则拒绝。
- Edge 1440px 与 390px 验收通过，无横向溢出、按钮裁切、图片失败、控制台错误或失败请求。

下一步：V0.3 模块 5“会员会话与设备安全”。按模块规则先完成设计与边界确认，再进入迁移和实现；模块 6 UI 升级不得提前混入。

## 16. 2026-07-31 修复记录：订单列表售后进度

- 修复订单项售后申请后，“售后中”标签按订单主状态查询而漏单的问题。
- 订单项售后继续保持独立状态，不把部分售后的订单主状态强制改为 `AFTER_SALE`。
- 订单列表按 `PENDING_REVIEW`、`APPROVED`、`RETURN_SHIPPED`、`REFUNDING` 活跃售后单计算 `displayStatus=AFTER_SALE`，并返回处理中数量、最新售后单、类型、状态和更新时间。
- 有活跃售后的订单不再重复出现在“已发货/已完成”，但仍保留在“全部”；旧整单 `mall_order.status=AFTER_SALE` 查询保持兼容。
- 商城订单卡片展示“售后待审核、等待寄回商品、退货运输中、退款处理中”等真实进度。

验证结果：后端 105 项测试通过；商城端生产构建和最终后端打包通过；真实订单 40 从“售后中”接口返回，保留 `status=SHIPPED` 并返回 `displayStatus=AFTER_SALE/latestAfterSaleStatus=APPROVED`；Edge 1440px/390px 均显示“等待寄回商品”，无横向溢出、按钮裁切、控制台错误或失败请求。

## 17. 2026-07-31 完成记录：V0.3 模块 5 会员会话与设备安全

V0.3 模块 5 已完成开发、迁移、回归、真实多设备链路和 Edge 桌面/移动端验收。

- 商城浏览器使用 `crypto.randomUUID()` 生成并在 `localStorage` 持久化 32 位随机设备标识，不采集硬件指纹；所有商城请求携带设备标识，登录 DTO 强制校验格式。
- 设备类型由后端 User-Agent 统一判定，平板计入移动端；设备、会话、主设备变更审计分别存储。
- 每位会员最多同时在线 2 台移动端和 1 台电脑端；同设备重新登录轮换 Token，新电脑替换旧电脑，第三台手机保留主手机并下线普通手机。
- 首台移动设备登记为主设备，退出登录后主设备身份不丢失；当前普通手机可通过独立短信用途 `PRIMARY_DEVICE_CHANGE` 更换主设备，30 天最多 3 次。
- Token 仅向客户端返回一次，MySQL 与 Redis 定位键只保存 SHA-256 摘要；退出、替换和远程下线在数据库事务提交后删除 Redis 会话。
- 商城个人中心新增登录设备列表、当前设备/主设备标识、远程下线和短信主设备更换；移动端展开验证表单后自动避让固定底部导航。
- 数据库迁移为 `sql/V2.12.0__mall_member_device_session.sql`，回滚脚本为 `sql/rollback/V2.12.0__mall_member_device_session_rollback.sql`。

验证结果：

- 后端全量测试 123 项通过，0 失败；最终后端 JAR 打包通过并连接 `mall_migration_test_20260728` 在 8080 启动，健康检查 `status=UP`。
- 商城用户端生产构建通过。
- V2.12.0 完成升级、回滚、再次升级和重复升级验证，结果依次为 3、0、3 张表，中文注释正常。
- 真实模拟 3 台手机和 2 台电脑后保持 2 个移动会话与 1 个桌面会话；被替换的普通手机和旧电脑均返回 401，新电脑正常；短信更换主设备、审计和远程下线通过。
- Edge 1440px/390px 均显示 3 个设备、唯一当前设备和唯一主设备，`scrollWidth` 等于视口宽度，无控制台错误或失败请求；移动端表单与固定导航保持 12px 间距。

下一步：V0.3 模块 6“商城用户端 UI 升级”。保持真实接口和现有业务能力，统一升级首页、列表、详情、购物车、结算、订单、地址簿、个人中心和登录；不得混入 V0.4 角色权限或运营端菜单调整。

## 18. 2026-07-31 完成记录：V0.3 模块 6 商城用户端 UI 升级

V0.3 模块 6 已按用户指定的 `preview-c.png` 纠正重做。未回滚前五个模块，继续复用 Vue 3、Vant、Pinia、Vue Router、真实业务接口、Store、路由和业务状态；未改动后端接口、若依运营端或 V0.4 菜单规划。

- 首页精确采用模型 C 的珊瑚红文案区、薄荷绿商品拼贴、黄色服务条和四列真实商品结构；导航、商品卡、订单详情物流横幅和个人中心入口同步使用同一视觉语法。
- 统一首页、商品列表、商品详情、购物车、结算、订单列表、订单详情、地址簿、个人中心和登录页的暖米黄背景、深色粗边、硬阴影、8px 圆角、字号层级和状态提示。
- 商品卡、订单卡、地址卡和表单控件增加克制的悬停上浮、图片缩放及按钮按压反馈；移动端关闭上浮和缩放，固定底部导航保留安全区留白。
- 增加键盘焦点可见样式，并统一支持 `prefers-reduced-motion: reduce`，减少动态效果时不影响业务操作。
- 页脚版本标记更新为 V0.3；`ai-web/`、视觉原型、原业务库 `ry-vue` 和运营端未修改。

验证结果：

- `npm.cmd run build`（`mall-storefront`）：通过，Vite 转换 375 个模块。
- `mvn.cmd -pl mall-business -am test`：123 项通过，0 失败。
- `http://localhost:5174/`、`http://localhost:8080/api/mall/health`、`http://localhost:8081/`：均可访问，商城健康检查返回 `code=200/status=UP`。
- 使用 Mock 验证码完成真实会员登录和加购，登录态购物车、结算、订单、个人中心与地址簿区域均正常读取真实接口。
- Edge 1440px/390px 检查首页、商品列表、商品详情、购物车、结算、订单列表、订单详情和个人中心；订单 40 的真实物流横幅、轨迹与售后记录显示正常，所有页面 `scrollWidth` 等于视口宽度，无图片失败、控制台错误、失败请求或底部导航遮挡。

模块 6 完成后，V0.3 全部六个模块均已完成；下一阶段按交接计划进入 V0.4，优先处理运营端“物流履约”和“库存管理”归入会员管理的信息架构调整。

## 19. 2026-07-31 补充记录：模块 6 C1+ 青春波普视觉定稿

商城用户端已按确认后的 `C1+` 方案完成视觉定稿：保留 C1 编号栏目结构，吸收 C2 的圆润粗体气质和 C3 的高对比商品标签，正文继续使用清晰黑体。桌面导航改为 `01-04` 编号栏目，移动底栏使用珊瑚红顶线、黄色选中块和粗体标签；商品图片标签改为黄色/薄荷绿实色、深色粗边和硬阴影。登录输入框移除浏览器蓝色细框，聚焦时使用白底和珊瑚红硬阴影；协议复选框固定为 `15×15px`，文字在窄屏自然换行。

验证结果：

- `npm.cmd run build`：通过，Vite 转换 375 个模块。
- `mvn.cmd -pl mall-business -am test`：123 项通过，0 失败。
- Edge 真实登录态复查首页、商品列表、购物车、结算、订单列表、订单 40 和个人中心，共 14 个桌面/移动端页面；横向溢出、坏图、控制台错误和失败请求均为 0，移动底栏贴合视口且无内容遮挡。
- 登录焦点计算样式为白底、`5px 5px` 珊瑚红硬阴影和 `outline: none`，协议复选框实测 `15×15px`。
- `5174` 商城端、`8080` 后端和 `8081` 若依运营端均已恢复运行，健康检查返回 `code=200/status=UP`。

当前状态不变：V0.3 六个模块全部完成，下一开发阶段为 V0.4；`ai-web/` 和原型目录未纳入本次修改或提交。

## 20. 2026-07-31 暂停记录：下周续开发入口

- 当前分支：`dev`；本轮正式基线：`f68a134 fix: 应用C1+青春波普视觉细节`，已推送至 `origin/dev`。
- V0.3 模块 1 至模块 6 已全部完成并验收，当前没有遗留开发待办；商城用户端最终视觉为已确认的 C1+“青春波普商店”。
- 本轮最后验证：商城生产构建 375 个模块通过，后端 123 项测试通过；Edge 登录态桌面/移动端 14 页无横向溢出、坏图、控制台错误或失败请求。
- 暂停时 `5174` 商城端、`8080` 后端和 `8081` 若依运营端仍在运行；下次如机器重启，按本文件“本地启动步骤”恢复。
- 下周从 V0.4 开始：先梳理若依运营端信息架构，将“物流履约”和“库存管理”规划归入“会员管理”业务分组；先确认菜单结构和静态模型，再实施菜单、路由、授权兼容及版本化 SQL 迁移。
- V0.4 继续遵守现有架构红线，不拆微服务、不引入 CQRS、不提前重建角色权限体系；`ai-web/`、两个原型目录和 `会话延续说明.md` 保持不提交。

## 21. 2026-08-03 修复记录：商城导航与商品排序视觉

进入 V0.4 前已完成两个商城 UI 缺陷修复，V0.4 业务开发仍未开始。

- 桌面导航采用用户确认的 B“连续分栏式”方案。根据首版截图反馈已去除表格式粗边框、单元格竖线和硬阴影，改为最大 720px 的轻量连续导航组；当前栏目使用浅黄底、珊瑚色编号和短底线，悬停栏目只显示薄荷绿背景。
- 新增 `StoreSortSelect.vue` 替换原生 `select`。桌面端使用深色粗边、硬阴影和黄/薄荷绿状态的自定义浮层，移动端自动切换为贴底选择面板并锁定背景滚动。
- 排序仍写入既有 `sort` URL 查询参数并调用真实商品接口；支持鼠标、方向键、Home/End、Enter 和 Escape，不改变商品 API 或后端逻辑。
- 901–1120px 隐藏导航英文副标并压缩间距，解决中等桌面宽度下品牌和栏目拥挤问题；900px 以下继续使用既有移动底栏。

验证结果：

- `npm.cmd run build`：通过，Vite 转换 376 个模块。
- `mvn.cmd -pl mall-business -am test`：123 项通过，0 失败。
- Edge 在 1091px 桌面宽度复现原截图场景：品牌完整、`scrollWidth == clientWidth`、活动栏目唯一；悬停首页时首页无红色底线，“全部商品”保持唯一活动状态。
- 截图反馈修正后补测 1366/1078/901/900px：桌面导航宽度分别不超过 720/640/542px，900px 正确切换移动布局；各断点均无横向溢出且活动栏目唯一。
- 桌面排序完成鼠标和键盘切换并更新 `sort=sales/priceDesc`；390×844 移动端面板贴合视口底部、显示 4 个选项并更新 `sort=priceAsc`。
- 验收过程无坏图、控制台错误或失败请求。临时导航/排序原型已吸收到正式代码，不纳入提交。

下一步保持不变：确认后再进入 V0.4 运营端信息架构设计。

## 22. 2026-08-03 完成记录：V0.4 模块 1 运营后台信息架构与职责权限

V0.4 模块 1 已完成并提交。正式运营后台继续建立在若依上，采用已确认的 A“业务域分组”方案，没有另建独立后台。

- 运营端新增运营职责、商品与库存、订单与履约、售后与资金、会员运营五个稳定业务入口，保留既有商城菜单 ID、路由、组件和权限标识。
- 新增运营主管、商品运营、客服售后、仓储履约、财务风控五类商城岗位，不新建另一套授权体系。
- 售后审核与退款执行拆分：客服售后可审核但不能退款，财务风控可执行已审核退款但不能审核；商品运营库存只读，仓储履约可调整库存和发货。
- 新增“运营职责”页面，按真实登录角色展示岗位职责、允许能力和禁止边界。
- 数据库迁移为 `sql/V2.13.0__mall_operations_role_structure.sql`，回滚脚本为 `sql/rollback/V2.13.0__mall_operations_role_structure_rollback.sql`。

验证结果：后端 124 项测试、若依生产构建、最终 JAR 打包、五条真实岗位业务 API、Edge 登录态桌面/窄屏检查均通过；V2.13.0 已完成升级、重复升级、回滚和再次升级验证。

提交状态：`3305161 feat: 完成V0.4运营后台职责权限重构` 已提交并推送，当前 `dev` 与 `origin/dev` 同步。

## 23. 2026-08-03 暂停记录：运营岗位测试账号修正

- 售后测试账号 `customer_service_test` 无法登录的根因已确认：登录名共 21 个字符，超过若依 `USERNAME_MAX_LENGTH=20` 的前置限制，因此尚未进入 BCrypt 密码校验就返回“用户不存在/密码错误”。
- 用户已重新创建合规账号 `customer_serV_test`，密码为 `1234567`，真实 `/login` 返回 `code=200`；`/getInfo` 返回角色 `mall_customer_service`。
- 新账号拥有会员查询、物流查询、售后查询与售后审核权限，不包含退款执行权限，符合客服售后岗位边界。
- 原超长账号已删除；诊断过程中临时创建的 `after_sales_test` 已停用，避免留下两个可用的客服售后测试账号。
- 当前五个有效岗位测试账号的密码统一为 `1234567`：
  - 运营主管：`ops_lead_test`
  - 商品运营：`product_ops_test`
  - 客服售后：`customer_serV_test`
  - 仓储履约：`fulfillment_test`
  - 财务风控：`finance_risk_test`
- 暂停时后端 `8080`、若依运营端 `8081`、商城用户端 `5174` 均在运行。

明日继续入口：从 V0.4 模块 2“订单运营中心”开始，先完成模块设计、职责边界和运营流程确认，再进行数据库迁移、后端测试、若依前端实现、真实角色链路与 Edge 验收；继续遵守一个模块一个提交及不修改 `ai-web/` 的规则。

## 24. 2026-08-04 完成记录：V0.4 模块 2 订单运营中心

V0.4 模块 2 已完成。订单运营中心定位为跨交易链路的统一只读查询与导航入口，不复制发货、退款或库存调整写操作；这些操作继续由物流、售后和库存原模块负责。

- 新增 `GET /mall/order-center` 分页查询，支持订单号、会员 ID、订单、支付、风控、物流、售后状态及创建时间范围筛选；新增 `GET /mall/order-center/{orderId}` 聚合详情。
- 详情聚合订单商品快照、支付记录、库存预占、物流单与轨迹、订单项售后、风控记录和订单操作日志，并在若依页面按五个标签页呈现。
- 订单中心可跳转既有发货工作台并自动带入订单号，也可跳转售后处理并自动打开指定售后详情；没有新增跨模块写接口。
- 运营主管、客服售后、仓储履约、财务风控具有列表与详情只读权限；商品运营保持无权访问，符合岗位职责边界。
- 数据库迁移为 `sql/V2.14.0__mall_order_operations_center.sql`，回滚脚本为 `sql/rollback/V2.14.0__mall_order_operations_center_rollback.sql`；已完成独立库升级、重复升级、回滚、再次升级及角色菜单验证。
- 后端风控筛选与数据库枚举统一为 `PENDING_CHECK/PASSED/REVIEW/REJECTED`，非法 `MANUAL_REVIEW` 会被服务端拒绝；创建时间单点边界可精确命中订单 40。

验证结果：后端全量 128 项测试通过，若依生产构建和最终 JAR 打包通过；订单 40 聚合出 1 个商品、1 条支付、1 条库存预占、2 个物流节点、1 条售后、1 条风控和 4 条操作记录。Edge 登录态在 1440px 与 390px 下完成五个详情标签页、物流跳转和售后跳转验收，页面级横向溢出、控制台错误和失败请求均为 0。

模块完成时 `8080` 后端、`8081` 若依运营端、`5174` 商城用户端和 `6379` Redis 均在运行。下一步等待用户确认后进入 V0.4 模块 3“商品与库存运营中心”，不得提前混入后续物流、售后资金、会员运营或全局工作台模块；继续不修改、不提交 `ai-web/` 和原型目录。

## 25. 2026-08-04 修复与计划补充：会员短信登录安全

用户确认将图形验证码加入后续计划，归入 V0.4“会员运营与登录安全”范围，不提前混入当前模块 3“商品与库存运营中心”。

- 已修复短信验证码 60 秒倒计时在进入“我的订单”并跳回登录页后立即重置的问题：成功发送后在当前浏览器会话中保存手机号和绝对截止时间，路由往返及页面刷新均按原截止时间恢复，不延长倒计时。
- 修复已通过 Edge 1366px/390px 的发送、订单鉴权跳转、手机号恢复和刷新恢复真实链路验证；商城生产构建通过。
- 采用风险触发式图形验证码：低风险请求可直接发送短信；手机号、IP、设备或短时间请求异常时，发送前要求完成滑块/拼图验证。
- 图形验证码必须由服务端生成、校验并签发一次性短期票据；发送短信接口只接受有效且未消费的票据，不能由前端单独判断通过。
- 保留短信发送的手机号 60 秒间隔、手机号/IP/设备频控、失败次数和每日上限；图形验证不能替代服务端限流。
- 桌面端采用居中验证弹层，移动端采用贴底面板，视觉需遵循已确认的 C1+ 青春波普风格，并提供刷新、失败重试和键盘/辅助操作的可用状态。
- 开发顺序固定为：先输出桌面端与移动端静态模型并确认，再设计挑战接口和持久化/限流规则，最后完成前后端真实链路、Edge 双端检查和单模块提交。

## 26. 2026-08-04 暂停记录：V0.4 模块 3、模块 4 及岗位首页同步

V0.4 模块 3“商品与库存运营中心”和模块 4“物流履约工作台”均已完成、验收、提交并推送，V0.4 当前完成进度为 4/7。

- 模块 3 新增只读商品库存中心，支持 SPU/SKU、上架和库存状态筛选、商品汇总、SKU 明细及库存流水入口；补齐 SKU 主键保留、履约占用删除拦截、服务端分页和低库存 SQL 筛选。
- 模块 3 权限边界：运营主管可操作商品和库存，商品运营可维护商品但库存只读，仓储履约可调整库存但不能维护商品，客服售后和财务风控不获得商品库存权限。
- 模块 4 将原发货页面升级为物流履约工作台，增加全局汇总、组合筛选、最新轨迹、履约阶段和三类运营关注信号；物流公司使用服务端白名单，继续保持一单一物流、手工 Adapter 和轨迹只追加。
- 模块 4 权限边界：运营主管、仓储履约可发货和追加轨迹，客服售后只读，商品运营和财务风控无物流权限；签收后只能追加更正说明，不能回退物流状态。
- 五类商城管理账号的若依首页已按真实角色展示岗位职责、最新权限同步、职责边界和已授权功能入口；超级管理员可切换查看全部岗位，普通账号只显示所属岗位。
- 岗位首页与“运营职责”页共用 `roles.js` 配置。以后每次权限变更都要同步更新该配置、真实 RBAC 迁移和验收记录，避免页面说明与实际权限不一致。
- V2.16.1 只为财务风控补齐订单中心父目录 3981，恢复其既有只读入口，不增加订单写权限。

验证与提交：

- 模块 3 后端全量 135 项通过；模块 4 后端全量 138 项通过；两次若依生产构建和最终后端打包均通过。
- V2.15.0、V2.16.0、V2.16.1 已完成升级、重复升级、回滚和再升级验证，测试库 `mall_migration_test_20260728` 最终保持升级状态。
- 五个岗位账号真实首页、功能入口和 Edge 桌面/移动端检查通过；验证码开关恢复为 `true`，五账号 `pwd_update_date` 均为 `NULL`。
- 提交依次为 `2518bd9 feat: 完成V0.4商品库存运营中心`、`f27df5f feat: 完成V0.4物流履约工作台`、`90a23ba feat: 在管理首页展示岗位职责`；`dev` 与 `origin/dev` 同步。
- 暂停时后端 `8080`、若依运营端 `8081`、商城用户端 `5174` 均在运行。

明日继续入口：从 V0.4 模块 5“售后资金与异常处理中心”开始，先确认客服审核、财务退款、异常支付、补偿任务和对账告警的职责边界及页面设计，再按迁移、后端测试、若依前端、真实岗位链路、Edge 双端验收和独立提交的顺序实施。风险触发式图形验证码继续保留到后续“会员运营与登录安全”模块，不提前混入模块 5；继续不修改、不提交 `ai-web/` 和原型目录。

## 27. 2026-08-05 完成记录：V0.4 模块 5 售后资金与异常处理中心

V0.4 模块 5 已完成实现与回归，运营后台仍建立在若依现有“售后与资金”业务域下，没有新增独立后台或角色体系。

- 新增 `GET /mall/after-sale-funds/center` 聚合接口，统一返回售后审核、退款执行、对账差异、异常告警和补偿任务的统计、分页工作队列及优先级信号。
- 聚合查询按权限收缩数据：客服只能看到售后工作项；财务可处理资金异常和补偿；运营主管拥有完整入口；商品运营、仓储履约不获得中心权限。服务端同步拒绝无权访问资金队列。
- 若依新增“售后资金处理中心”页面：六项统计、工作队列切换、状态/单号/订单/时间筛选、详情抽屉，以及跳转售后、对账和告警专用页；补偿任务支持按权限重试和运营主管手动执行到期任务。
- 对账页支持中心跳转传入的 `tab`、`businessNo`，告警查询也按业务单号过滤，保留原有写接口和操作审计边界。
- V2.17.0 新增中心菜单及补偿任务查询、重试、手动执行权限；角色授权为运营主管 5 项、客服 2 项、财务 4 项，商品运营和仓储履约 0 项。回滚只删除本版本菜单和授权。
- 新增设计文档、后端服务/Mapper/单测、若依 API 与页面；未修改 `ai-web/`、原型目录和商城用户端。

验证结果：

- `mvn.cmd -pl mall-business -am clean test`：141 项通过，0 失败。
- `npm.cmd run build:prod`（若依前端）：构建通过；仅保留项目原有的 piano 导出警告和资源体积提示。
- V2.17.0 测试库升级、重复升级、回滚、再次升级验证通过，菜单/授权计数依次为 5/11、5/11、0/0、5/11。
- 当前本地服务保持 `8080` 后端、`8081` 若依前端、`5174` 商城端运行；后续需在浏览器中用五个岗位账号完成中心入口、客服数据收缩、财务资金队列和 390×844 布局复核。

当前进度：V0.4 模块 1 至模块 5 已完成，进度 5/7。下一模块为会员运营与登录安全，风险触发式图形验证码按既定顺序先做静态模型确认，再进入挑战接口和限流实现；继续遵守一个模块一个提交、不修改 `ai-web/` 和原型目录的规则。

## 28. 2026-08-05 修复记录：售后资金中心分页与菜单编码

- 根因：若依启用 PageHelper 参数分页，中心查询参数包含 `pageNum/pageSize` 时会自动追加 `LIMIT`；Mapper 又手写分页，最终生成重复 `LIMIT` 并报 SQL 语法错误。
- 修复：移除中心工作项 SQL 的手写 `LIMIT`，交由 PageHelper 统一分页；真实登录调用 `/mall/after-sale-funds/center` 已返回 `code=200`。
- 根因：V2.17.0 迁移通过 PowerShell 文本管道执行时，中文菜单字节被系统代码页转换为问号。
- 修复：迁移菜单名称和备注改为 UTF-8 十六进制 `convert(... using utf8mb4)`；测试库 4000-4004 已恢复正常中文，验证码开关恢复为 `true`。
- 修复后后端全量测试 141 项通过，JAR 已重新打包并启动，8080 健康检查通过。

## 29. 2026-08-05 补充记录：模块 5 职责下发

- 五类岗位首页均同步售后资金中心职责：运营主管完整处理，财务风控执行退款/对账/告警/补偿，客服售后只读聚合并审核售后。
- 商品运营和仓储履约首页明确资金中心权限关闭，分别继续只负责商品可售状态、库存和物流履约。
- `roles.js` 岗位说明、运营职责矩阵与 V2.17.0 RBAC 保持一致，未新增商品运营或仓储履约授权。

## 30. 2026-08-05 完成记录：V0.4 模块 6 会员运营与登录安全

- 已确认 B 方案：风险请求使用居中拼图验证弹层，移动端保持贴底面板适配；静态原型已在方案确认后清理，确认结果已吸收到真实商城登录页。
- 新增 `V2.18.0__mall_member_login_security.sql` 和回滚脚本，测试库 `mall_migration_test_20260728` 已完成升级与重复升级，临时验证库已完成回滚。
- 新增 `POST /api/mall/member/sms-challenge` 和 `/sms-challenge/verify`；挑战记录落 MySQL，风险窗口计数落 Redis，服务端签发一次性短期票据。
- `/sms-code` 保留手机号 60 秒重发、手机号/IP/设备频控、失败次数和每日上限；风险触发时只返回 `challengeRequired`，短信接口必须消费有效票据后才发送。
- 商城登录页已接入 B 方案弹层，覆盖加载、换图、错误重试、票据发送和移动端布局；未修改 `ai-web/` 及既有 V0.3 原型目录。
- 后端全量测试 146 项通过，商城 `npm.cmd run build` 通过；接口链路已验证挑战创建、正确验证、短信发送和票据重放失败。

## 31. 2026-08-05 修复记录：会员登录短信按钮真实页面验收

- 自动化接口和构建最初均通过，但真实商城登录页点击“获取验证码”时发现 Vue 方法绑定 `@click="sendCode"` 会把点击事件对象作为 `challengeTicket` 传给 Axios，后端因此报 `JSON parse error: Cannot deserialize value of type java.lang.String from Object value`。
- 修复 `mall-storefront/src/views/AccountView.vue` 为显式调用 `@click="sendCode()"`，确保首次请求体中的 `challengeTicket` 始终为空字符串。
- 使用 Edge 内核无头浏览器实际打开 `http://localhost:5174/account` 并操作：首次发送请求返回 200；连续风险请求后页面显示 B 方案拼图弹层；错误位置保留弹层并显示重试提示；正确位置返回一次性 ticket，携票发送短信成功；同 ticket 重放被服务端拒绝。
- 修复后再次执行商城构建和会员认证单测，分别为 379 个模块构建通过、5 项测试通过；本次修复单独提交。

## 32. 2026-08-05 修复记录：拼图块视觉缺失

- 真实截图复核发现拼图弹层只有缺口和滑块指示，场景内没有明确显示被拖动的拼图块；同时拼图块、山体和场景背景引用了未定义的 `--coral`、`--peach` 变量，导致视觉上接近空白。
- 在场景中增加随 `smsChallengePosition` 移动的拼图块，并补齐 C1+ 主题色变量；服务端 `targetX` 和验证逻辑保持不变。
- Edge 桌面端截图确认拼图块、缺口、山体和背景均可见；390×844 移动端确认弹层贴底、拼图块可见、`scrollWidth=390`，无控制台错误和失败请求。

## 33. 2026-08-05 完成记录：V0.4 模块 7 运营工作台与全局验收

- 新增 `GET /mall/operations/dashboard`，若依首页复用现有 `mall:operations:roles` 权限，按岗位返回今日订单、销售额、待付款、待发货、低库存、售后、退款、对账、告警和补偿指标。
- 指标查询继续沿用 `Controller -> Service -> Mapper`，客服/仓储不会看到资金异常数据；没有新增后台体系，也未修改 `ai-web/`、原业务库或既有商城用户端业务接口。
- 运营主管真实登录后首页显示完整 11 项指标；商品运营、客服售后、仓储履约、财务风控五个账号逐一调用真实接口核对，权限裁剪与职责矩阵一致。
- Edge 真实浏览器验收：桌面 `1440×900`、移动 `390×844` 均无横向溢出、控制台错误或失败请求；运营首页真实显示接口数据并可跳转业务入口。
- 后端 `mvn.cmd -pl mall-business -am test`：148 项通过；商城 `npm.cmd run build` 和若依 `npm.cmd run build:prod` 均通过，仅保留项目既有构建警告。

当前进度：V0.4 模块 1 至模块 7 已完成，进度 7/7。V0.4 全部模块已完成真实接口、Edge 桌面/移动端验收；下一步等待用户确认是否进入 V0.5 规划。

## 34. 2026-08-05 补充验收：五岗位页面级权限核对

- 使用 Edge 内核分别打开五个岗位首页并读取真实渲染结果：五个账号的指标均与服务端权限裁剪一致。
- 五个桌面页面均为 `scrollWidth=1440`，无控制台错误和失败请求；此前运营主管移动端 `390×844` 也已验证通过。

## 35. 2026-08-05 全线回归修复：物流默认时间回拨

- 全线真实回归发现：运营端未填写物流节点时间时直接采用服务器当前时间；系统时钟轻微回拨后，新节点可能被误判为早于上一节点并返回 500。
- 修复后仅对服务端默认时间进行保护：若默认时间早于上一节点，则沿用上一节点时间；运营人员显式填写更早时间仍按原规则拒绝。
- 新增确定性回归测试覆盖服务器时钟落后于上一节点 1 分钟的场景；后端全量测试增至 149 项并全部通过，最终 JAR 打包成功。
- 真实接口使用订单 `M20260805154814941952` 验证：将运输中节点推进 1 分钟后，不填写时间追加派送中和已签收均成功，会员确认收货完成，未再出现 500。

## 36. 2026-08-05 暂停记录：V0.5 规划及模块 1 静态模型确认

- V0.5 正式开发顺序确定为：首页内容运营、会员消息中心、账号与隐私生命周期、优惠券闭环、商品评价、收藏与浏览足迹、经营分析与全局验收。
- 模块 1 范围确认：运营内容覆盖 C1+ 主视觉、公告条、推荐专区和商品排序；主视觉保持左侧文案与右侧双图拼贴，不改为通用轮播。
- 发布规则确认：支持草稿、预览、定时发布和手动停用；推荐商品人工优先，商品下架或库存失效后按销量自动补位。
- 权限确认：运营主管和商品运营可以查看、编辑、预览和发布；客服售后、仓储履约、财务风控不显示页面内容菜单，服务端同步执行 RBAC 校验。
- 重点验收行为确认：未发布、未到生效时间和已过期内容不展示；下架商品不展示；非法内部跳转由服务端拒绝；无有效配置时商城首页必须降级为可用状态。
- 新建一次性静态模型目录 `prototypes/mall-v0.5-content-operations/`，包含商城桌面/移动首页及 A/B/C 三种若依后台结构，并使用 Edge 无头模式完成桌面和移动截图复核。
- 最终 UI 决策为 **B + A**：B“内容清单”作为页面内容管理首页，A“预览工作台”作为新增/编辑页；C 页面自由编排不采用，首页区块顺序保持固定。
- 当前仅完成需求、结构和静态模型确认，尚未创建 V2.19.0 迁移、后端内容模块、若依正式页面或商城真实首页接口。明日从模块 1 的接口设计和首个 TDD 纵向切片开始。

## 37. 2026-08-06 完成记录：V0.5 模块 1 首页内容运营首轮实现

- 已创建并应用 `sql/V2.19.0__mall_home_content_operations.sql`，回滚脚本同步使用 `4010-4016` 菜单编号，避开既有 V0.4 `4000-4004` 菜单；测试库完成回滚、再升级和重复升级，最终保持升级状态。
- 已新增 `mall-business` 首页内容模块：主视觉、公告条、推荐专区独立保存与排期，发布时拒绝同类型时间重叠；服务端拒绝非法内部目标、非商城资源图片、重复或非正数 SPU。
- 已将正式管理页面实现于若依 `ruoyi-ui/src/views/mall/content/index.vue`，采用 **B 内容清单 + A 编辑/预览工作台**；页面内容权限仅对运营主管和商品运营生效，客服、仓储、财务服务端访问返回 403。
- 商城 `HomeView.vue` 已接入匿名 `/api/mall/homepage`，人工推荐商品失效时按销量补位，无有效内容时保持原 C1+ 降级首页可用。
- 真实验证：运营主管账号 `ops_lead_test` 登录后发布主视觉、公告条和推荐专区成功；商城首页聚合返回三类内容；同类型重叠发布被拒绝；客服账号 `customer_serV_test` 访问内容列表被拒绝。
- 验证结果：后端首页内容测试 6 项通过，商城端和若依端生产构建通过，最新后端 JAR 打包成功；若依 `http://localhost:8081`、商城 `http://localhost:5174` 已启动供页面复核。

当前进度：V0.5 模块 1 已完成首轮实现与真实接口验收，尚未提交 Git；下一步进行页面级桌面/移动复核、补充必要测试后再提交模块 1，完成后才进入会员消息中心。

## 38. 2026-08-06 首页三类内容交互边界确认

- 主视觉继续使用后台定时排期切换，不改为普通自动轮播。
- 公告条移动端仅支持手势横向滑动，不增加自动播放计时器；桌面端保持并列展示。
- 推荐专区继续使用商品网格，不改为轮播。
- 商城端已将公告条拆分为独立滑动项，移动端使用横向滚动和吸附定位。

## 39. 2026-08-06 首页同步与开发验证码修复

- 修复商城首页接口重复拼接 `/api` 的问题：Axios `baseURL` 已是 `/api`，首页请求改为 `/mall/homepage`，后台发布内容现在能被用户端读取。
- 开发环境短信 Mock 验证码默认恢复为 `123456`；重启后的 `8080` 后端已实际发送验证码并用 `123456` 登录验证成功。
- 回归检查：错误路径返回 `401`，正确路径返回 `200` 且包含后台更新时间 `2026-08-06 11:15:00`；商城端生产构建通过。

## 40. 2026-08-06 V0.5 模块 2 方案确认：会员消息中心

- 用户确认采用静态模型 **B 业务会话聚合**，因为按业务分组后信息更集中、不凌乱。
- 正式首页固定为订单消息、物流提醒、售后进度、账户安全四组业务会话，并显示每组未读数；分类内再查看消息列表和详情。
- 消息由订单、支付、物流、售后和账户安全模块通过 `MemberMessagePort` 写入；会员端只允许访问当前会员自己的消息，跳转路径必须由服务端白名单校验。
- 本模块验收仍需完成迁移升级/回滚、后端行为测试、商城构建、真实接口链路以及 Edge 桌面和移动端实测后再提交 Git。

当前进度：V0.5 模块 1 已提交完成；模块 2 已完成 B 方案确认，正在开始数据模型和后端首个纵向切片。

## 41. 2026-08-06 完成记录：V0.5 模块 2 会员消息中心

- 新增 `V2.20.0__mall_member_messages.sql` 和回滚脚本；独立测试库 `mall_migration_test_20260728` 已完成回滚、升级和重复升级，原库 `ry-vue` 未修改。
- 新增 `MemberMessagePort`，由订单、支付、物流、订单项售后和会员会话模块写入消息，保持 `Controller -> Service -> Mapper -> MySQL` 和跨模块 Port 边界。
- 新增 `/api/mall/member/messages/summary`、分类列表、详情、单条已读和全部已读；四类会话固定为 `ORDER`、`LOGISTICS`、`AFTER_SALE`、`ACCOUNT`，会员隔离和跳转白名单由服务端执行。
- 商城新增 `/messages` B 方案页面：业务会话聚合首页、分类消息列表、移动端底部详情弹层、未读数徽标和全部已读；桌面导航和移动 TabBar 均已接入消息入口。
- 后端测试从 155 项增至 160 项并全部通过；商城 `npm.cmd run build` 通过；新后端 JAR 已打包并在 8080 连接测试库运行。
- 真实验收：新会员登录自动生成账户安全消息；摘要/列表返回四组；单条已读重复调用幂等；跨会员消息 ID 返回不存在；Edge `1440x900` 和 `390x844` 均无横向溢出、控制台错误或失败请求，移动消息详情底部弹层可见。

当前进度：V0.5 模块 1、模块 2 已完成并待提交本次变更；下一模块为模块 3“账号与隐私生命周期”。
# 最新暂停点：V0.5 模块 3 账号与隐私生命周期（2026-08-06）

当前模块采用已确认的 **A 总览结构 + C 生命周期时间轴**。本轮正式代码尚未提交，明天必须在现有工作区继续，禁止重置或覆盖。

已完成：

- 静态模型位于 `prototypes/mall-v0.5-account-privacy/`，桌面和移动模型已确认。
- 新增 `V2.21.0__mall_member_account_lifecycle.sql` 及回滚脚本，包含手机号更换请求、账号操作短期票据、生命周期审计和会员停用时间。
- 测试库 `mall_migration_test_20260728` 已完成真实回滚和再升级验证：回滚计数 `0,0`，升级后为三张表和一列，即 `3,1`；当前测试库保持 V2.21.0 升级状态。
- 后端已新增账号概览、授权记录、在线设备数、未完成订单、处理中售后和生命周期查询；跨订单/售后模块使用 `MemberOrderStatePort`、`MemberAfterSaleStatePort`。
- 已新增两阶段手机号更换：旧手机号验证码、新手机号验证码、短期票据、手机号唯一性检查，成功后全部设备下线。
- 已新增账号注销：资格复检、当前手机号验证码、立即停用和全部会话下线；未完成订单或处理中售后会硬拦截；交易、售后和财务凭证不物理删除。
- 商城端新增 `/account/privacy` 正式页面并接入真实 API，使用 A 总览 + C 时间轴结构；个人中心已增加“账号与隐私”入口。
- 后端全量 `mall-business` 测试曾通过 `167` 项；商城端 `npm.cmd run build` 通过；真实链路使用测试会员 `18800008607` 完成短信发送、注册登录和 `GET /api/mall/member/profile/account-lifecycle`，均返回 `200`。
- 若依后台只读接口已开始接线：`MallMemberAccountQueryService.overviewForAdmin` 和 `GET /mall/member/{memberId}/account-lifecycle` 已加入代码，但尚未重新编译和制作会员列表查看界面。

明天从这里继续：

1. 先重新运行 `MallMemberAccountQueryServiceTest` 和 `mvn.cmd -pl mall-business -am test`，确认今天最后的若依只读接口修改可编译。
2. 完成 `ruoyi-ui` 会员列表中的“账号与隐私”只读抽屉/对话框；手机号默认脱敏，不提供后台任意注销能力。
3. 复核换号和注销写入会员消息；如尚未接入则通过既有 `MemberMessagePort` 补齐。
4. 使用独立测试会员真实跑通完整换号三步和注销资格拦截；避免注销已有业务测试会员。
5. 重打包并常驻启动 8080，启动 8081 和 5174；执行 Edge `1440x900`、`390x844` 实地页面检查、交互检查、控制台和失败请求检查。
6. 更新两份进度文档为最终结果，复核 diff 后提交并推送 `dev`。

运行状态说明：本轮为真实链路临时启动的 8080 Java 任务会随 PowerShell 任务结束，不保证当前仍在运行；5174/8081 也应在明天开工时重新检查端口。开发验证码仍为 `123456`，MySQL 凭据通过安全环境变量注入，不写入交接文档。
# 暂停前复核（2026-08-06 17:21）

- 串行复跑 `MallMemberAccountQueryServiceTest`、`MallMemberAccountLifecycleServiceTest`：6 项全部通过。
- `mvn.cmd -pl ruoyi-admin -am -DskipTests compile`：通过；`npm.cmd run build`（商城端）：通过。
- 上一条记录中的“若依只读接口尚未编译”已更新为“已编译通过”；会员列表 UI 与正式 JAR 重启仍留到明天。

# V0.5 模块 3 完成记录（2026-08-07）

- 若依会员列表已新增“账号与隐私”只读抽屉，展示脱敏手机号、账号状态、在线设备、注销资格、授权记录和生命周期时间轴；后台不提供任意注销权限。
- 生命周期查询已合并 `mall_member_lifecycle_audit`，换号和注销审计现在会真实出现在时间轴中；手机号更换成功会通过 `MemberMessagePort` 写入账户安全消息并跳转 `/account/privacy`，注销只保留审计凭证。
- 新增手机号更换消息跳转白名单 `/account/privacy`，并补充账号生命周期、消息白名单和安全消息写入测试。
- 后端 `mvn.cmd -pl mall-business -am test`：169 项通过；若依端 `npm.cmd run build:prod`：通过；商城端 `npm.cmd run build`：通过；后端 `mvn.cmd -pl ruoyi-admin -am package -DskipTests`：通过。
- 真实换号链路会员 `18800008608` -> `18800008618`：旧号验证、新号验证、换号完成均返回 200，旧令牌立即 401，新号登录成功；数据库确认 `PHONE_CHANGED` 审计和账户安全消息已写入。
- 真实注销链路会员 `18800008609`：资格检查通过、注销验证码发送和注销返回 200，注销后令牌 401，数据库状态为停用并记录 `ACCOUNT_DEACTIVATED`。
- 最新服务已启动：商城后端 `8080`、若依后台 `8081`、商城端 `5174`、Redis `6379`；`GET /api/mall/health` 返回 200/UP。Edge 已打开商城账号页、账号隐私页和若依登录页，并完成桌面/移动截图检查。
- 移动端补充了账号隐私页头部可收缩、长文案断行和 360px 最大内容宽度，避免真实移动 CSS 视口下页面横向溢出。

当前进度：V0.5 模块 3 已完成迁移、后端、商城端、若依只读视图、真实换号/注销链路和构建验收，下一步进入 Git 提交后的 V0.5 模块 4“优惠券闭环”。

## 42. 2026-08-07 完成记录：V0.5 模块 6 收藏与浏览足迹

- 新增 `V2.24.0__mall_member_product_activity.sql` 和回滚脚本；测试库完成升级、回滚、再升级和重复升级验证，最终保持升级状态。
- 新增 `/api/mall/member/product-activity` 接口组，覆盖收藏汇总、列表、状态、添加、取消，以及足迹列表、记录、单删、批删和清空。
- 收藏与足迹均保存商品快照；商品下架或删除后记录继续保留，返回 `available=false`，商城端显示“已失效”且不提供商品跳转或购买入口。
- 商城商品详情新增收藏按钮并为登录会员自动记录足迹；个人中心新增数量摘要和“收藏与足迹”快捷入口；`/activity` 使用双标签组织收藏和按日期分组的足迹。
- 真实接口验证重复收藏幂等、重复浏览计数、跨会员隔离、失效记录保留、批量删除和清空均通过。
- 后端全量测试 190 项通过；商城与若依生产构建、后端打包均通过；Edge 桌面 `1440×900` 和移动 `390×844` 无横向溢出、控制台错误或失败请求。

当前进度：V0.5 模块 1 至模块 6 已完成，进度 6/7。下一模块为模块 7“经营分析与全局验收”；继续遵守一个模块一个提交、不修改 `ai-web/`、V0.6 再开始小程序端设计与实现的规则。

## 43. 2026-08-07 暂停记录：下周继续 V0.5 模块 7

- 本周工作已在提交 `d7e7aa1 feat(member): add favorites and browsing history` 收尾，模块 6 不再有未提交业务改动。
- 当前工作区仅保留用户已有未跟踪目录 `ai-web/`，禁止修改、暂存或提交；`dev` 比 `origin/dev` 领先 7 个提交，尚未推送。
- 最新后端 JAR 已重新打包并启动，`8080` 健康检查为 `UP`；若依后台 `8081`、商城端 `5174` 也在监听。
- 下周从 V0.5 模块 7“经营分析与全局验收”开始：先确认经营分析指标、权限边界和静态页面需求，再按迁移、后端、若依端、商城端真实链路、Edge 桌面/移动端检查和独立提交执行。
- 保留当前测试库 `mall_migration_test_20260728` 的 V2.24.0 升级状态；开发验证码为 `123456`，MySQL 凭据通过安全环境变量注入。

## 44. 2026-08-10 完成记录：V0.5 模块 7 经营分析与全局验收

- 若依商城运营下新增独立“经营分析中心”，采用紧凑运营看板结构，提供 `7/30/90` 天周期、指标卡、销售与订单趋势、商品销售排行及岗位经营摘要；不新建独立后台。
- 新增 `GET /mall/business-analytics`，继续遵循 `Controller -> Service -> Mapper -> MySQL`。接口只返回订单、商品、会员、履约、售后和资金聚合，不返回会员手机号、昵称、订单号或其他明细。
- 权限按真实角色键在服务端收缩：运营主管和超级管理员查看全部；商品运营查看销售汇总与商品；客服查看会员聚合与售后；仓储查看订单量、库存和履约；财务查看销售、退款、净收入和资金异常。
- 岗位首页 `roles.js` 已同步新增经营分析职责、功能入口和权限矩阵，页面说明与 V2.25.0 RBAC 保持一致。
- 新增 `V2.25.0__mall_business_analytics.sql` 及回滚脚本；测试库完成升级、岗位授权检查、回滚、排序恢复和再次升级，最终保持升级状态。
- 后端全量 `mall-business` 测试 `196` 项通过；若依生产构建和最终后端 JAR 打包通过。构建仅保留项目原有 piano 导出与资源体积警告。
- 五个岗位测试账号逐一真实登录并调用接口，返回板块与职责完全一致；Edge 桌面检查覆盖五岗位，运营主管另完成 `390x844` 移动检查，页面横向溢出、控制台错误和失败请求均为 `0`，趋势图 canvas 正常生成。
- 一次性静态模型已在结构结论吸收到正式页面后删除；`ai-web/` 未修改、未暂存。

当前进度：V0.5 模块 1 至模块 7 全部完成，进度 `7/7`。下一步进入 V0.5 全线最终复核、提交和推送；V0.6 才开始小程序端设计与实现。

## 45. 2026-08-10 修复记录：优惠券运营菜单中文化

- 修复 V2.22.0 将优惠券父菜单、管理菜单及功能按钮名称写成英文，导致若依侧栏显示 `Coupon Operations` 和 `Coupon Management` 的问题。
- 新增 `V2.25.1__mall_coupon_menu_chinese.sql` 与回滚脚本，将 4020 至 4026 菜单统一为“优惠券运营、优惠券管理、查询、新增、修改、发布或暂停、手工发放”。
- 独立测试库已完成升级、回滚和再次升级；最终 7 条菜单英文名称计数为 `0`，数据库保持中文升级状态。

## 46. 2026-08-10 完成记录：商城运营 C 方案菜单重组

- 按确认的 C“工作台优先”方案新增 `运营工作台` 目录，将 `运营首页`、`经营分析`、`岗位职责` 收为三个子入口；商城岗位和超级管理员登录 `/index` 后自动进入 `/mall/overview/home`。
- 商城运营侧栏隐藏外层重复“首页”，保留非商城角色的若依首页兼容路由；商城面包屑从“商城运营”开始，页签不再固定显示重复首页。
- 商品与库存、订单与履约、售后与资金、会员运营保持业务域折叠；页面内容、优惠券、商品评价合并为 `内容与营销`，子项为商城首页配置、优惠券管理、评价审核。
- 新增 `V2.25.2__mall_operations_navigation_restructure.sql` 及回滚脚本，保留旧菜单 ID 和功能权限；旧优惠券/评价目录仅停用隐藏，回滚可恢复原父子关系。
- 若依生产构建通过；`mall-business` 全量测试 `196` 项通过；测试库完成升级、回滚、重复升级并保持 V2.25.2 状态。
- 运营主管真实登录调用 `/getRouters` 返回 C 菜单树；Edge 正式页面桌面 `1440x900`、移动 `390x844` 均通过，工作台卡片、中文菜单、默认跳转、横向溢出、控制台错误和失败请求检查通过。
- C 方案静态模型和临时检查脚本已删除，`ai-web/` 仍未修改、未暂存。

## 47. 2026-08-10 完成记录：HTTP 错误状态码统一

- 修复发布验收中的 P1：旧有 `AjaxResult.code` 为 `400-599` 时同步写入真实 HTTP 状态码；成功和业务警告继续保持 HTTP 200，JSON `code/msg` 契约不变。
- `GlobalExceptionHandler` 已统一越权 `403`、参数 `400`、方法 `405`、媒体类型 `415`、路由资源 `404` 和未知异常 `500`；认证失败入口现在直接返回 `401`；不存在订单详情明确返回 `404`。
- 新增 `HttpStatusResponseBodyAdvice` 和 `HttpStatusResponseContractTest`，若依后台测试共 `7` 项通过；商城业务全量测试 `197` 项通过。
- 新 JAR 已在安全环境变量注入下启动于 `8080`。真实接口验证结果：匿名/失效商城令牌 `401`、若依岗位越权 `403`、不存在订单 `404`、Swagger/Druid 诊断路径 `404`、不支持方法 `405`、健康接口 `200`。
- 修正 `scripts/mall-storefront-e2e.ps1` 的非 2xx 响应读取逻辑；商城用户端真实 E2E `18/18` 通过。Edge 桌面 `1366x900`、移动 `390x844` 页面无失败请求、页面错误和横向溢出。
- 验收报告已更新为 HTTP 状态码 P1 已修复；剩余发布整改为迁移重复执行幂等性和前端依赖漏洞。受保护未知商城 API 匿名访问仍会先由认证过滤器返回 `401`，属于安全层优先行为，暂不作为本次阻断项。
- 本次未提交 Git；`ai-web/` 未修改、未暂存。后端当前运行 PID 约为 `29640`，商城端 `5174`、若依端 `8081` 保持原运行状态。

明天从这里继续：

1. 处理迁移版本历史/重复升级幂等性，补充全量迁移回归验证。
2. 复核并制定商城端、若依端 high/critical 依赖升级或构建隔离方案。
3. 完成头像专项和设备上限自动化安全测试后，再进行最终发布签署和 Git 推送。

## 48. 2026-08-11 完成记录：数据库迁移历史与幂等执行器

- 新增 `scripts/invoke-db-migrations.ps1` 与 `scripts/MallMigration.psm1`，统一发现正向 `Vx.y.z__*.sql`、按语义版本排序并排除 `_rollback.sql`；不再由发布人员手工逐个重复运行迁移。
- 执行器自动维护 `mall_schema_history`，记录版本、脚本、SHA-256、执行人、耗时和成功状态；使用带过期时间的 `mall_schema_lock` 数据库租约锁阻止跨机器并发迁移。重复版本、失败历史、脚本缺失、乱序和校验和漂移会快速失败。
- 已有商城表但无历史记录时默认拒绝执行，必须人工确认实际版本后显式传入一次 `-BaselineVersion`；空库不得伪造基线。MySQL 密码只从 `RUOYI_DATASOURCE_PASSWORD` 读取，不进入命令行参数或仓库。
- 新增无外部依赖静态测试和受控真实库集成测试。测试库 `mall_migration_test_20260728` 已登记 `0.2.0-2.25.2` 共 40 个基线版本，连续执行两次均保持最新状态，`mall_order.risk_status` 保持 1 列。
- 独立临时空库在导入若依与 Quartz 基础结构后，执行器按语义版本一次应用全部 40 个正向迁移，第二次执行全部跳过；历史为 `40/40/0`，`risk_status` 仍为 1 列。临时库已删除。
- 临时 `2.25.3` 探针完成首次迁移、重复跳过和校验和漂移阻断验证；并发执行器在 1 秒锁等待后被拒绝；无效 `2.25.4` SQL 被拒绝并保留失败历史，证明盲重试会被阻断。测试结束后探针表、租约和测试历史均已清理，测试库恢复为 40 条成功基线记录，未修改业务数据。
- `scripts/release.ps1` 现在将正向迁移、执行器和 SHA-256 清单打入 `database/` 发布目录；安全启动说明、数据库接入说明和全线验收报告已同步。

当前发布整改状态：图形验证码、生产默认凭据、HTTP 状态码和迁移幂等性均已完成。下一步处理前端依赖漏洞，之后补齐头像专项、设备上限自动化和最终发布安全复验。
