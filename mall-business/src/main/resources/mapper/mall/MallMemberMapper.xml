<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mall.member.mapper.MallMemberMapper">
    <resultMap id="MemberResult" type="com.ruoyi.mall.member.domain.MallMember">
        <id property="memberId" column="member_id"/>
        <result property="phone" column="phone"/><result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/><result property="status" column="status"/>
        <result property="registerSource" column="register_source"/><result property="registerIp" column="register_ip"/>
        <result property="lastLoginIp" column="last_login_ip"/><result property="lastLoginTime" column="last_login_time"/>
        <result property="createBy" column="create_by"/><result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/><result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <sql id="memberColumns">member_id,phone,nickname,avatar,status,register_source,register_ip,last_login_ip,last_login_time,create_by,create_time,update_by,update_time,remark</sql>
    <select id="selectById" resultMap="MemberResult">select <include refid="memberColumns"/> from mall_member where member_id=#{memberId} and del_flag='0'</select>
    <select id="selectByIdForUpdate" resultMap="MemberResult">select <include refid="memberColumns"/> from mall_member where member_id=#{memberId} and del_flag='0' for update</select>
    <select id="selectByPhone" resultMap="MemberResult">select <include refid="memberColumns"/> from mall_member where phone=#{phone} and del_flag='0' limit 1</select>
    <select id="selectList" resultMap="MemberResult">select <include refid="memberColumns"/> from mall_member where del_flag='0'<if test="phone!=null and phone!=''"> and phone like concat('%',#{phone},'%')</if><if test="nickname!=null and nickname!=''"> and nickname like concat('%',#{nickname},'%')</if><if test="status!=null and status!=''"> and status=#{status}</if> order by member_id desc</select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="memberId">insert into mall_member(phone,nickname,status,register_source,register_ip,last_login_ip,last_login_time,create_by,create_time,del_flag) values(#{phone},#{nickname},#{status},#{registerSource},#{registerIp},#{lastLoginIp},#{lastLoginTime},#{createBy},now(),'0')</insert>
    <update id="update">update mall_member set nickname=#{nickname},avatar=#{avatar},status=#{status},update_by=#{updateBy},update_time=now(),remark=#{remark} where member_id=#{memberId} and del_flag='0'</update>
    <update id="updateNickname">update mall_member set nickname=#{nickname},update_by=#{updateBy},update_time=now() where member_id=#{memberId} and del_flag='0'</update>
    <update id="updateAvatar">update mall_member set avatar=#{avatar},update_by=#{updateBy},update_time=now() where member_id=#{memberId} and del_flag='0'</update>
    <update id="updateLoginInfo">update mall_member set last_login_ip=#{lastLoginIp},last_login_time=#{lastLoginTime},update_time=now() where member_id=#{memberId}</update>
    <update id="updatePhone">update mall_member set phone=#{phone},update_time=now() where member_id=#{memberId} and status='0' and del_flag='0'</update>
    <update id="deactivate">update mall_member set status='1',deactivated_time=now(),update_time=now() where member_id=#{memberId} and status='0' and del_flag='0'</update>
    <update id="deleteByIds">update mall_member set del_flag='2',update_time=now() where member_id in <foreach collection="array" item="id" open="(" separator="," close=")">#{id}</foreach></update>
</mapper>
