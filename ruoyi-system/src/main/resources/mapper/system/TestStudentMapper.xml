<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.system.mapper.TestStudentMapper">

  <resultMap type="TestStudent" id="TestStudentResult">
      <result property="studentId" column="student_id"/>
      <result property="studentName" column="student_name"/>
      <result property="age" column="age"/>
      <result property="status" column="status"/>
      <result property="createTime" column="create_time"/>
      <result property="updateTime" column="update_time"/>
      <result property="remark" column="remark"/>
  </resultMap>

      <sql id="selectTestStudentVo">
        SELECT student_id, student_name, age, status,
               create_time, update_time, remark
        FROM test_student
    </sql>

    <select id="selectTestStudentById"
            parameterType="Long"
            resultMap="TestStudentResult">
        <include refid="selectTestStudentVo"/>
        WHERE student_id = #{studentId}
    </select>

    <select id="selectTestStudentList"
            parameterType="TestStudent"
            resultMap="TestStudentResult">
        <include refid="selectTestStudentVo"/>
        <where>
            <if test="studentName != null and studentName != ''">
                AND student_name LIKE concat('%', #{studentName}, '%')
            </if>
            <if test="age != null">
                AND age = #{age}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

     <insert id="insertTestStudent"
            parameterType="TestStudent"
            useGeneratedKeys="true"
            keyProperty="studentId">
        INSERT INTO test_student (
            student_name,
            age,
            status,
            create_time,
            remark
        )
        VALUES (
            #{studentName},
            #{age},
            #{status},
            #{createTime},
            #{remark}
        )
    </insert>

    <update id="updateTestStudent" parameterType="TestStudent">
        UPDATE test_student
        <set>
            <if test="studentName != null and studentName != ''">
                student_name = #{studentName},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            update_time = #{updateTime},
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
        WHERE student_id = #{studentId}
    </update>

    <delete id="deleteTestStudentById" parameterType="Long">
        DELETE FROM test_student
        WHERE student_id = #{studentId}
    </delete>

    <delete id="deleteTestStudentByIds">
        DELETE FROM test_student
        WHERE student_id IN
        <foreach
            item="studentId"
            collection="array"
            open="("
            separator=","
            close=")">
            #{studentId}
        </foreach>
    </delete>


</mapper>



