<view wx:if="{{loading}}" class="state"><text>正在加载</text></view>
<view wx:elif="{{error}}" class="state"><text>{{error}}</text><button bindtap="retry">重新加载</button></view>
<view wx:elif="{{empty}}" class="state"><text>这里暂时没有内容</text></view>
