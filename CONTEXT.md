# 单商户 B2C 商城

面向普通消费者销售自营实物商品的单商户商城。商城负责从商品展示、下单支付到履约售后的完整交易过程。

## 商品

**SPU**：
用户认知中的商品主体，承载名称、品牌、分类和图文详情，不直接表示某个可购买规格。
_避免使用_：产品、货品

**SKU**：
具有明确规格组合、销售价格和库存数量的最小可售单元。
_避免使用_：规格商品、子商品

**可用库存**：
当前尚未被订单占用、允许新订单锁定的 SKU 数量。

**锁定库存**：
已被待付款订单暂时占用，但尚未确认为销售扣减的 SKU 数量。

**待付款保留期**：
订单创建后为会员持续锁定库存的 30 分钟固定期限。继续支付或支付失败不会延长期限，支付成功转为销售扣减，取消或超时关闭后释放库存。
_避免使用_：可续期库存锁定

## 交易

**会员**：
在用户端拥有身份并可创建订单的消费者。
_避免使用_：客户、商城用户

**身份验证短信**：
为完成会员主动发起的注册、登录或更换手机号而发送的一次性验证码消息，不包含营销内容。
_避免使用_：通知短信、营销短信

**授权同意记录**：
记录会员对某个版本协议或特定个人信息处理活动作出同意的可审计凭证。

**会员会话**：
会员在一台设备上的一次持续登录状态。每位会员最多同时保留 2 个移动端会话和 1 个电脑端会话，平板计入移动端。
_避免使用_：管理员会话、无限设备登录

**主移动设备**：
会员指定的常用移动设备，在移动端会话超额时受到保留。新移动设备登录会自动下线普通移动设备，不挤出主移动设备。
_避免使用_：永久设备、电脑端主设备

**浏览器设备**：
由商城网页生成随机标识的一次浏览器安装实例，用于区分会员会话。更换浏览器、清除网站数据或使用无痕窗口后视为新设备，不采集侵入性硬件指纹。
_避免使用_：硬件设备、永久设备指纹

**订单**：
会员一次结算产生的交易单据，记录收货信息、金额和履约状态。

**订单项**：
订单中某个 SKU 的交易快照，是计算金额和发起售后的最小单位。
_避免使用_：订单明细商品

**支付单**：
针对订单的一次支付尝试及其结果记录，与订单本身的履约状态分开管理。

支付单必须在订单待付款保留期结束前创建；按时创建的支付单可以在保留期结束后返回最终结果，但保留期结束后不得发起新的支付尝试。

**支付结果等待期**：
在待付款保留期内创建的支付单可额外等待最终结果的 5 分钟期限。等待期结束仍无结果时关闭订单并释放库存，订单库存最长锁定 35 分钟。
_避免使用_：无限支付等待

**异常支付**：
订单关闭并释放库存后才确认成功的支付结果。异常支付不恢复原订单，款项应原路退回会员。
_避免使用_：复活订单、迟到订单

**订单快照**：
下单时复制并固定的商品、规格、价格和收货信息，来源数据后续变化不会修改该快照。

**业务幂等键**：
客户端为一次下单意图生成的唯一标识，相同会员使用同一标识重复请求时只能得到同一处理结果。

## 交易风控

**刷单**：
以制造虚假销量、套取权益或操纵经营数据为目的的非真实交易行为。

**恶意占库存**：
通过频繁创建未支付订单长期占用可售库存、影响正常会员购买的行为。

**风险状态**：
订单经过风险检测和人工审核的处理状态，与付款、发货等订单业务状态分别管理。

**风险信号**：
用于判断交易异常程度的可解释事实，例如高频下单、共享设备、频繁取消或地址聚集。
_避免使用_：刷单证据

## 履约与售后

**物流单**：
订单发货后产生的承运信息及物流节点集合。一个订单最多对应一个物流单，订单内商品不拆分为多个包裹发货。
_避免使用_：快递单

**手工物流**：
由运营人员录入物流公司、运单号并维护物流轨迹节点的履约方式，不调用第三方实时物流服务。
_避免使用_：真实物流、实时物流

**物流轨迹节点**：
物流过程在某个时间点发生的状态事实，分为已发货、运输中、派送中、已签收、运输异常和更正说明。节点可以按实际履约过程跳过中间状态，但事件时间不得早于上一节点；发布后不可改写或删除，录入错误通过追加更正节点说明。
_避免使用_：可编辑物流记录

**确认收货**：
会员确认已收到商品，使待收货订单进入已完成状态。物流签收后会员未操作时，系统在 7 天后自动确认收货。
_避免使用_：物流签收

**售后单**：
会员针对订单项发起的仅退款或退货退款处理单据，拥有独立于订单的状态。
_避免使用_：退款申请

**仅退款**：
无需寄回商品，审核通过后直接退还相应款项的售后类型。

**退货退款**：
会员需寄回商品，商家确认收货后退还相应款项的售后类型。

**退货物流**：
退货退款审核通过后，会员寄回商品所形成的承运信息。运单提交后不可直接改写，录入错误通过客服追加更正记录处理。
_避免使用_：原订单物流

**售后可申请数量**：
订单项购买数量扣除售后处理中和已退款成功数量后，当前仍可申请售后的数量。质量问题允许申请全部可申请数量，全量申请本身不构成风险信号。
_避免使用_：默认恶意退货数量

**售后资格期限**：
订单项允许发起售后的固定截止时间。撤销售后单或申请被驳回不会重新计算或延长该期限。
_避免使用_：重新申请期限
